// Plan 05-05 Task 5 RED stub. GREEN replaces with useQuery(['wallet','balance']
// + ['wallet','threshold']) + role-conditional CTA + sessionStorage dismiss.

'use client';

export interface LowBalanceBannerProps {
  roles: string[];
}

export function LowBalanceBanner(_props: LowBalanceBannerProps): React.ReactElement | null {
  return null;
}
