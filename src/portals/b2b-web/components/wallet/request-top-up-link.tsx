// Plan 05-05 Task 5 RED stub. GREEN replaces with mailto subject-only link.

'use client';

export interface RequestTopUpLinkProps {
  adminEmail?: string;
}

export function RequestTopUpLink(_props: RequestTopUpLinkProps): React.ReactElement {
  return <a href="#">stub</a>;
}
