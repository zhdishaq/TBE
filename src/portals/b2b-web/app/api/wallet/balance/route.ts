// Plan 05-02 Task 3 RED stub -- /api/wallet/balance.
// Minimal placeholder so the TDD RED gate sees a failing route contract.

export const runtime = 'nodejs';

export async function GET() {
  return new Response(null, { status: 501 });
}
