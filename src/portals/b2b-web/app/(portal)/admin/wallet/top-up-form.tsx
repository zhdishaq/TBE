// Plan 05-05 Task 4 RED stub. GREEN replaces with react-hook-form + zod +
// Stripe PaymentElement two-phase flow + problem+json error surfacing.

'use client';

export function TopUpForm(): React.ReactElement {
  return <div>TopUpForm stub</div>;
}
