---
context: phase
phase: 04-b2c-portal-customer-facing
stage: planned-not-executed
plans: 5
waves: 3
status: ready-to-execute
last_updated: 2026-04-15T22:30:16.899Z
---

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|---------------------|
| Silent reversal of locked CONTEXT decisions | Initial planner switched D-08/D-10 (single PaymentIntent + partial capture) to two-PaymentIntent strategy without flagging. Caught by plan-checker. | blocking | When any plan/task action contradicts a CONTEXT D-XX entry, STOP and either (a) implement per CONTEXT, or (b) escalate to /gsd-discuss-phase for explicit superseding decision. Never silently flip. |
| Cross-plan URL contract drift | 04-02 used ?booking=, 04-03 wanted ?hotelBookingId=, 04-04 used ?basketId= — no plan owned reconciliation. | advisory | Shared frontend contracts (URL params, event names, DTO shapes) must be owned by exactly one plan with explicit files_modified entries on the consuming plan's pages. |

<current_state>
Phase 4 planning is COMPLETE and verified. 5 PLAN.md files exist (04-00..04-04) covering all 21 phase REQ-IDs across 3 waves. Plan checker passed on iteration 2 with 0 blockers / 0 warnings. STATE.md updated to "Ready to execute". No code has been written yet — execution has not started.
</current_state>

<completed_work>

- 04-CONTEXT.md — locked decisions D-01..D-16
- 04-UI-SPEC.md — design contract
- 04-RESEARCH.md — 18 pitfalls, library pins verified 2026-04-16, Open Questions RESOLVED (commit 9622f61)
- 04-PATTERNS.md — 58/65 in-repo analogs mapped
- 04-VALIDATION.md — nyquist_compliant: true, 16-row per-task verification map populated
- 04-00-PLAN.md through 04-04-PLAN.md — 5 plans, all gates passed (commit a3283eb)
- STATE.md — recorded planning completion (5 plans, Ready to execute)
</completed_work>

<remaining_work>

- Execute Wave 1: 04-00 (Wave 0 scaffold)
- Execute Wave 2: 04-01, 04-02, 04-03 (parallelizable)
- Execute Wave 3: 04-04 (capstone — depends on 04-00 + 04-02 + 04-03)
</remaining_work>

<decisions_made>

- **Single combined Stripe PaymentIntent** with `capture_method=manual` + sequential partial captures (`AmountToCapture`, `FinalCapture=false` then `true`) — honors locked D-08/D-10. Two-PI alternative explicitly rejected.
- **Unified checkout URL contract** `?ref={kind}-{id}` via `lib/checkout-ref.ts` (parseCheckoutRef/buildCheckoutRef). Implemented in 04-04 Task 3b which modifies 04-02 checkout pages.
- **Hotel voucher PDF**: streaming pass-through from NotificationService only (D-16 single source of truth). Local regeneration in BookingService removed.
- **Frontend stack pinned** (verified 2026-04-16): Auth.js v5.0.0-beta.31 (EXACT pin — still beta), Next 16.1.6, @stripe/stripe-js 9.2.0, @stripe/react-stripe-js 6.2.0, nuqs 2.8.9, @tanstack/react-query 5.99.0, react-day-picker 9.14.0, vitest 2.x, Playwright 1.x.
- **Metronic starterKit kept as .jsx** with `allowJs:true`; new code in .ts/.tsx (Pitfall 17).
- **Playwright moved to `<wave_verify>`**; per-task `<automated>` is vitest/dotnet only (Nyquist 120s feedback budget).
- **04-04 wave: 3** with `depends_on: ["04-00", "04-02", "04-03"]` — explicit ordering for EF migrations + MassTransit consumers + payment-element-wrapper reuse.
</decisions_made>

<blockers>
None. Ready to execute.
</blockers>

## Human Actions Pending

1. **[BLOCKING for 04-02]** Manual Keycloak admin provisioning of `tbe-b2c-admin` service client (client_credentials grant, view-users + manage-users roles). Required by 04-00 Task 3 user_setup; 04-02 Task 3 has `<pre_flight>bash infra/keycloak/verify-audience-smoke.sh</pre_flight>` that will fail until done.
2. **[BLOCKING for Wave 0 smoke]** Stripe test-mode keys + webhook signing secret in `.env.test`.

## Required Reading (in order)

1. `.planning/phases/04-b2c-portal-customer-facing/04-CONTEXT.md` — LOCKED user decisions (D-01..D-16). Do not contradict.
2. `.planning/phases/04-b2c-portal-customer-facing/04-RESEARCH.md` — 18 documented pitfalls (esp. Pitfall 1 Auth.js beta, 3 edge-split, 9 Stripe partial-capture, 11 await params, 14 PDF streaming, 16 CSP, 17 jsx/tsx coexistence)
3. `.planning/phases/04-b2c-portal-customer-facing/04-UI-SPEC.md` — frontend visual/interaction contract
4. `.planning/phases/04-b2c-portal-customer-facing/04-PATTERNS.md` — file-by-file analog map (58/65 in-repo)
5. `.planning/phases/04-b2c-portal-customer-facing/04-VALIDATION.md` — per-task verification map + Wave 0 prerequisites
6. `.planning/phases/04-b2c-portal-customer-facing/04-00-PLAN.md` — Wave 1 scaffold (start here)

## Infrastructure State

- Backend (.NET 8): Phase 1-3 complete. Stripe.net 51.x, QuestPDF, RazorLight, SendGrid, MassTransit, EmailIdempotencyLog, BookingService read endpoints, Keycloak realms (`tbe-b2c`, `tbe-staff`, `tbe-agent`) all in place.
- Frontend (Next.js): Metronic starterKit lives at `ui/` (mostly .jsx, no Auth.js/Stripe/vitest/playwright yet).
- Keycloak: `tbe-b2c-admin` service client NOT yet provisioned (manual step).
- DB: EF Core migrations through Phase 3. Phase 4 adds Baskets table (04-04 Task 1 migration `20260416150000`) + Car migration (04-04 Task 3a `20260416170000`).

<context>
Phase 4 is mostly a wiring/gluing phase — backend already has Stripe, PDF, email, saga from Phases 1-3. Phase 4 adds: B2C frontend, Auth.js v5+Keycloak OIDC, Baskets table + BasketPaymentOrchestrator, ReceiptsController, HotelVoucher consumer, CarBookingsController, Trip Builder UI. Critical risk: Auth.js v5 is still beta — Wave 0 (04-00) must establish framework + smoke before downstream waves can proceed in parallel.
</context>

<next_action>
Run `/clear` then `/gsd-execute-phase 4` to begin Wave 1 (04-00 scaffold). Verify the two human-actions-pending items (Keycloak admin client + Stripe test keys) before Wave 2 starts.
</next_action>
