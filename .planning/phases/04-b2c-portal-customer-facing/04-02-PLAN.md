---
phase: 04-b2c-portal-customer-facing
plan: 02
type: execute
wave: 2
depends_on: ["04-00"]
files_modified:
  - src/portals/b2c-web/app/page.tsx
  - src/portals/b2c-web/app/flights/page.tsx
  - src/portals/b2c-web/app/flights/results/page.tsx
  - src/portals/b2c-web/app/flights/[offerId]/page.tsx
  - src/portals/b2c-web/app/checkout/layout.tsx
  - src/portals/b2c-web/app/checkout/details/page.tsx
  - src/portals/b2c-web/app/checkout/payment/page.tsx
  - src/portals/b2c-web/app/checkout/processing/page.tsx
  - src/portals/b2c-web/app/checkout/success/page.tsx
  - src/portals/b2c-web/app/api/search/flights/route.ts
  - src/portals/b2c-web/app/api/airports/route.ts
  - src/portals/b2c-web/components/search/flight-search-form.tsx
  - src/portals/b2c-web/components/search/airport-combobox.tsx
  - src/portals/b2c-web/components/search/passenger-selector.tsx
  - src/portals/b2c-web/components/search/date-range-picker.tsx
  - src/portals/b2c-web/components/search/cabin-class-picker.tsx
  - src/portals/b2c-web/components/results/flight-card.tsx
  - src/portals/b2c-web/components/results/filter-rail.tsx
  - src/portals/b2c-web/components/results/sort-bar.tsx
  - src/portals/b2c-web/components/results/search-results-panel.tsx
  - src/portals/b2c-web/components/checkout/stepper.tsx
  - src/portals/b2c-web/components/checkout/email-verify-gate.tsx
  - src/portals/b2c-web/components/checkout/payment-element-wrapper.tsx
  - src/portals/b2c-web/components/checkout/passenger-details-form.tsx
  - src/portals/b2c-web/lib/stripe.ts
  - src/portals/b2c-web/lib/search-params.ts
  - src/portals/b2c-web/lib/query-client.ts
  - src/portals/b2c-web/hooks/use-flight-search.ts
  - src/portals/b2c-web/tests/search/airport-combobox.test.tsx
  - src/portals/b2c-web/tests/search/passenger-selector.test.tsx
  - src/portals/b2c-web/tests/search/use-flight-search.test.ts
  - src/portals/b2c-web/tests/checkout/payment-element-wrapper.test.tsx
  - src/portals/b2c-web/tests/checkout/email-verify-gate.test.tsx
  - src/portals/b2c-web/e2e/flight-booking.spec.ts
  - src/portals/b2c-web/e2e/flight-booking-mobile.spec.ts
  - src/services/InventoryService/InventoryService.API/Controllers/AirportsController.cs
  - src/services/InventoryService/InventoryService.Application/Airports/IataAirportSeeder.cs
  - src/services/InventoryService/InventoryService.Application/Airports/IAirportLookup.cs
  - src/services/InventoryService/InventoryService.Infrastructure/Airports/RedisAirportLookup.cs
  - src/services/InventoryService/InventoryService.API/Program.cs
  - data/iata/airports.dat
  - data/iata/README.md
autonomous: true
requirements: [B2C-03, B2C-04, B2C-05, B2C-06, NOTF-02]  # NOTF-02 reused path — flight path confirmation email (NOTF-01 already shipped Phase 3); B2C-06 PaymentElement mechanism shared with hotel plan but owned here

must_haves:
  truths:
    - "A user can visit / and use a search form with IATA autocomplete (min 2 chars, debounced 200ms), date range picker (react-day-picker), passenger selector (adult/child/infant-on-lap/infant-in-seat with airline rules), and cabin class picker"
    - "Submitting the search navigates to /flights/results with search params in the URL (nuqs) and the initial fetch runs server-side; the response is cached in TanStack Query and filter/sort run client-side without refetching"
    - "Result cards show airline + segments + times + duration + stops + all-in price with YQ/YR separated from taxes per FLTB-03"
    - "Selecting a result → /checkout/details (passenger details form with RHF+zod) → /checkout/payment (Stripe PaymentElement) → /checkout/processing (polls saga status) → /checkout/success"
    - "Checkout blocks at /checkout/payment if `email_verified` is false — a modal prompts 'Verify your email first' with a working Resend button (uses /api/auth/resend-verification from 04-01)"
    - "Mobile viewport completes the booking in exactly 5 steps: search → results → select → details → payment (confirmation is post-checkout and does NOT count per B2C-05 / D-14)"
    - "The Stripe PaymentElement is mounted ONLY on /checkout/payment; card data never reaches the portal server (COMP-01)"
    - "The /checkout/processing page polls `/baskets/{id}/status` (or `/bookings/{id}/status`) until terminal state — it does NOT treat the Stripe return_url redirect as success (Pitfall 6, FLTB/PAY D-12)"
    - "IATA airport autocomplete is backed by Redis (seeded at InventoryService startup from OpenFlights CSV per D-18) and never hits an external API per keystroke"
  artifacts:
    - path: "src/portals/b2c-web/components/search/flight-search-form.tsx"
      provides: "Search form with all required fields (B2C-03, FLTB-01)"
    - path: "src/portals/b2c-web/lib/search-params.ts"
      provides: "nuqs parsers for origin/destination/dates/pax/cabin/sort/filters (D-11)"
    - path: "src/portals/b2c-web/hooks/use-flight-search.ts"
      provides: "TanStack Query hook keyed by search criteria only; filter/sort apply client-side over cache (D-12)"
    - path: "src/portals/b2c-web/components/results/flight-card.tsx"
      provides: "Stacked card layout with YQ/YR/tax breakdown (D-13, FLTB-03)"
    - path: "src/portals/b2c-web/components/checkout/payment-element-wrapper.tsx"
      provides: "Stripe PaymentElement with confirmPayment + return_url=/checkout/processing (B2C-06, Pitfall 6)"
    - path: "src/portals/b2c-web/app/checkout/processing/page.tsx"
      provides: "Polls saga status every 2s with 90s cap (Pitfall 6)"
    - path: "src/portals/b2c-web/components/checkout/email-verify-gate.tsx"
      provides: "Modal fired on mount of /checkout/payment when email_verified=false (D-06, Pitfall 7)"
    - path: "src/services/InventoryService/InventoryService.Application/Airports/IataAirportSeeder.cs"
      provides: "BackgroundService seeding Redis from data/iata/airports.dat at startup"
    - path: "src/services/InventoryService/InventoryService.API/Controllers/AirportsController.cs"
      provides: "GET /airports?q=LON typeahead (D-18)"
    - path: "data/iata/airports.dat"
      provides: "OpenFlights airport dataset (CC-BY-SA attribution in README)"
  key_links:
    - from: "src/portals/b2c-web/hooks/use-flight-search.ts"
      to: "src/portals/b2c-web/lib/search-params.ts"
      via: "useQueryStates(searchParsers); queryKey derives from q.from/q.to/q.dep/… (NOT filters)"
      pattern: "useQueryStates\\(searchParsers\\)"
    - from: "src/portals/b2c-web/components/checkout/payment-element-wrapper.tsx"
      to: "Stripe Elements + /checkout/processing"
      via: "stripe.confirmPayment({ elements, confirmParams: { return_url: .../checkout/processing?basket=... } })"
      pattern: "return_url.*checkout/processing"
    - from: "src/portals/b2c-web/app/checkout/processing/page.tsx"
      to: "BookingService /bookings/{id}/status"
      via: "Polled every 2s with 90s cap; only terminal events advance UI"
      pattern: "bookings/\\$\\{.*\\}/status"
    - from: "src/portals/b2c-web/components/search/airport-combobox.tsx"
      to: "/api/airports?q=…"
      via: "debounced fetch with AbortController; min 2 chars"
      pattern: "AbortController"
    - from: "src/services/InventoryService/InventoryService.Infrastructure/Airports/RedisAirportLookup.cs"
      to: "Redis"
      via: "StackExchange.Redis SortedSet/Hash prefix lookup"
      pattern: "IDatabase|IConnectionMultiplexer"
---

<objective>
Ship the flight product end-to-end: search form → results → select → details → payment → processing → success. Build the client-side search stack (nuqs + TanStack Query + URL-state), Stripe PaymentElement wired to Phase 3 PaymentService (reuses booking saga; no new saga code), and the IATA airport typeahead (Redis-backed, seeded from OpenFlights). The email-verification gate fires at the payment step (D-06, Pitfall 7). PaymentElement is mounted ONLY on /checkout/payment (Pitfall 5). Booking success is derived from webhook-driven saga terminal state polled via /checkout/processing — NEVER from Stripe's client-side redirect (Pitfall 6, FLTB/PAY D-12).

Purpose: B2C-03, B2C-04, B2C-05, B2C-06 — the first full bookable product. NOTF-02 email path is inherited from Phase 3 (NOTF-01 flight confirmation already landed).
Output: A working flight booking end-to-end on mobile and desktop.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/04-b2c-portal-customer-facing/04-CONTEXT.md
@.planning/phases/04-b2c-portal-customer-facing/04-RESEARCH.md
@.planning/phases/04-b2c-portal-customer-facing/04-PATTERNS.md
@.planning/phases/04-b2c-portal-customer-facing/04-UI-SPEC.md
@.planning/phases/04-b2c-portal-customer-facing/04-00-PLAN.md
@src/services/PaymentService/PaymentService.Application/Stripe/StripePaymentGateway.cs
@src/services/BookingService/BookingService.API/Controllers/BookingsController.cs
@ui/starterKit/app/(layouts)/layout-1/page.jsx

<interfaces>
Gateway endpoints consumed (existing from Phase 2/3):
- `POST /search/flights` — body { from, to, dep, ret?, adults, children, infantsLap, infantsSeat, cabin } → { offers: FlightOfferDto[] ≤200 }
- `GET /airports?q=LON` — new in this plan: → [{ iata: "LHR", name: "London Heathrow", city: "London", country: "GB" }, …] max 20
- `POST /bookings` — body { offerId, passengers, productType: "flight" } → 202 { bookingId, paymentIntentAmount, currency }
- `POST /bookings/{id}/payment-intent` — creates Stripe PaymentIntent for single-product booking; returns { client_secret, amount, currency }
- `GET /bookings/{id}/status` — returns { status: "Initiated" | "Authorizing" | "TicketIssued" | "Confirmed" | "Failed" | ..., bookingReference?, pnr? }

Stripe (public):
- `@stripe/stripe-js`: `loadStripe(publishableKey)` — returns `Promise<Stripe | null>`
- `@stripe/react-stripe-js`: `<Elements stripe={promise} options={{ clientSecret }}>`, `<PaymentElement />`, `useStripe()`, `useElements()`

Auth.js v5 (from 04-00):
- `auth()` returns Session | null with `email_verified: boolean` — drive the verify gate.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: IATA airport typeahead — OpenFlights data + Redis seeder + /airports endpoint</name>
  <files>
    data/iata/airports.dat,
    data/iata/README.md,
    src/services/InventoryService/InventoryService.Application/Airports/IataAirportSeeder.cs,
    src/services/InventoryService/InventoryService.Application/Airports/IAirportLookup.cs,
    src/services/InventoryService/InventoryService.Infrastructure/Airports/RedisAirportLookup.cs,
    src/services/InventoryService/InventoryService.API/Controllers/AirportsController.cs,
    src/services/InventoryService/InventoryService.API/Program.cs
  </files>
  <read_first>
    src/services/InventoryService/InventoryService.API/Program.cs,
    src/services/BookingService/BookingService.API/Program.cs,
    .planning/phases/04-b2c-portal-customer-facing/04-RESEARCH.md
  </read_first>
  <action>
    1. **data/iata/airports.dat** — download OpenFlights airports.dat (8,000+ entries). Commit the file. Add `data/iata/README.md` citing source URL (`https://raw.githubusercontent.com/jpatokal/openflights/master/data/airports.dat`), CC-BY-SA 3.0 license, attribution text, and the 14-column schema.
    2. **IAirportLookup.cs** (Application): `Task<IReadOnlyList<AirportDto> SearchAsync(string prefix, int limit, CancellationToken ct)`. `AirportDto(string Iata, string Name, string City, string Country)`.
    3. **RedisAirportLookup.cs** (Infrastructure) — uses `IConnectionMultiplexer`. Storage scheme: Hash `iata:airports` keyed by IATA code → JSON; Sorted Set `iata:idx:prefix` with entries `"{prefix}|{IATA}"` scored by 0 (prefix index for `SortedSetRangeByValueAsync` prefix scan). Search algorithm: lowercase `prefix`; scan the sorted set for entries starting with that prefix; take first `limit`; HGET each IATA from `iata:airports`.
    4. **IataAirportSeeder.cs** (Application `BackgroundService`) — on startup: if `iata:seed:done` flag is absent or `FORCE_RESEED=true`, read `data/iata/airports.dat`, parse CSV (skip rows without valid IATA), for each row `(iata, name, city, country)` add Hash entry + add prefix index entries for `iata.lower()`, `city.lower()[0..n]` for `n=2..min(len,8)`. Set `iata:seed:done`. Log row count. Idempotent: re-running is a no-op.
    5. **AirportsController.cs** — `[ApiController, Route("airports")]` (NOTE: **no** `[Authorize]` — autocomplete is public per CONTEXT "Anonymous users can browse and search"). `[HttpGet]` `Get(string q, int limit = 10, CancellationToken ct)`: validate `q` length ≥2 and ≤8 (anti-abuse), cap `limit` at 20, call `IAirportLookup.SearchAsync`, return JSON. Add a light rate-limit attribute (AspNetCore `RequireRateLimiting` with a "airports" policy — 60 req/min/IP — add policy in Program.cs).
    6. **Program.cs** (InventoryService): register `IConnectionMultiplexer` if not already (use `Redis:ConnectionString` from config), `services.AddSingleton<IAirportLookup, RedisAirportLookup>()`, `services.AddHostedService<IataAirportSeeder>()`, rate-limiter policy `airports` (fixed window 60/min).
    7. **Frontend `/api/airports` pass-through** — `src/portals/b2c-web/app/api/airports/route.ts` — GET handler that `gatewayFetch`es `/airports?q={q}`; this is a PUBLIC endpoint so it uses a simpler fetch (no `auth()` required — but still same-origin). Adds Next.js cache header `cache: "default"` with short `s-maxage=60` to ride edge cache.
  </action>
  <verify>
    <automated>dotnet test tests/Inventory.Tests --filter "FullyQualifiedName~Airport" -warnaserror 2>/dev/null || dotnet build src/services/InventoryService/InventoryService.API -warnaserror</automated>
  </verify>
  <acceptance_criteria>
    - `test -s data/iata/airports.dat` (file non-empty).
    - `grep -c 'CC-BY-SA' data/iata/README.md` returns ≥ 1 (attribution).
    - `grep -c 'IAirportLookup' src/services/InventoryService/InventoryService.Application/Airports/IAirportLookup.cs` returns ≥ 1.
    - `grep -c 'BackgroundService' src/services/InventoryService/InventoryService.Application/Airports/IataAirportSeeder.cs` returns 1.
    - `grep -cE 'SortedSet|HashSet' src/services/InventoryService/InventoryService.Infrastructure/Airports/RedisAirportLookup.cs` returns ≥ 1.
    - `grep -c 'AddHostedService<IataAirportSeeder>' src/services/InventoryService/InventoryService.API/Program.cs` returns 1.
    - `grep -cE '\\[Authorize\\]' src/services/InventoryService/InventoryService.API/Controllers/AirportsController.cs` returns 0 (public endpoint — verified).
    - `grep -cE 'RateLimit|FixedWindow' src/services/InventoryService/InventoryService.API/Program.cs` returns ≥ 1 (anti-abuse).
    - Smoke test in running dev env: `curl "http://localhost:5000/airports?q=lon"` returns JSON array containing `LHR`.
  </acceptance_criteria>
  <done>
    Airport typeahead is live, seeded at service boot, and survives restarts without reseeding.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Search UI — form, results, URL state, client-side filter/sort</name>
  <files>
    src/portals/b2c-web/app/page.tsx,
    src/portals/b2c-web/app/flights/page.tsx,
    src/portals/b2c-web/app/flights/results/page.tsx,
    src/portals/b2c-web/app/flights/[offerId]/page.tsx,
    src/portals/b2c-web/app/api/search/flights/route.ts,
    src/portals/b2c-web/components/search/flight-search-form.tsx,
    src/portals/b2c-web/components/search/airport-combobox.tsx,
    src/portals/b2c-web/components/search/passenger-selector.tsx,
    src/portals/b2c-web/components/search/date-range-picker.tsx,
    src/portals/b2c-web/components/search/cabin-class-picker.tsx,
    src/portals/b2c-web/components/results/flight-card.tsx,
    src/portals/b2c-web/components/results/filter-rail.tsx,
    src/portals/b2c-web/components/results/sort-bar.tsx,
    src/portals/b2c-web/components/results/search-results-panel.tsx,
    src/portals/b2c-web/lib/search-params.ts,
    src/portals/b2c-web/lib/query-client.ts,
    src/portals/b2c-web/hooks/use-flight-search.ts,
    src/portals/b2c-web/tests/search/airport-combobox.test.tsx,
    src/portals/b2c-web/tests/search/passenger-selector.test.tsx,
    src/portals/b2c-web/tests/search/use-flight-search.test.ts
  </files>
  <read_first>
    ui/starterKit/components/ui/command.jsx,
    ui/starterKit/components/ui/popover.jsx,
    ui/starterKit/components/ui/card.jsx,
    ui/starterKit/components/ui/select.jsx,
    ui/starterKit/app/(layouts)/layout-1/page.jsx,
    .planning/phases/04-b2c-portal-customer-facing/04-UI-SPEC.md,
    .planning/phases/04-b2c-portal-customer-facing/04-RESEARCH.md,
    .planning/phases/04-b2c-portal-customer-facing/04-PATTERNS.md
  </read_first>
  <behavior>
    - `passenger-selector.test.tsx`: infants-on-lap ≤ adults enforced; total pax ≤ 9; increment/decrement buttons disable at limits; validation copy shown inline (not toast).
    - `airport-combobox.test.tsx`: debounces 200ms before fetching; minimum 2 chars before query; renders "LHR — London Heathrow" format; aborts previous request when new keystroke fires.
    - `use-flight-search.test.ts`: changing sort param does NOT trigger refetch (TanStack queryKey stable); changing origin triggers refetch; staleTime=90_000.
    - `flight-search-form.tsx`: submit navigates with full URL state; validation rejects past dates + from==to.
    - `flight-card.tsx`: renders all-in price with `incl. taxes` label AND shows fare breakdown on expand (base / YQ-YR / taxes separated — FLTB-03).
  </behavior>
  <action>
    **RED:** author the three test files to assert the behaviors above. Run `pnpm test` — must fail. Commit: `test(04-02): search form + results behaviors`.

    **GREEN:**
    1. **lib/search-params.ts** — copy RESEARCH Pattern 3 verbatim: `parseAsString/parseAsIsoDate/parseAsInteger/parseAsArrayOf/parseAsStringLiteral`. All 14 params typed (from, to, dep, ret, adt, chd, infl, infs, cabin, stops, airlines, timeWindow, price, sort). Defaults: `cabin=economy`, `sort=price`, counts 1/0/0/0.
    2. **lib/query-client.ts** — factory for TanStack `QueryClient` with `defaultOptions.queries.staleTime: 90_000` (selection-phase TTL, PITFALLS #11), `refetchOnWindowFocus: false` (results pages shouldn't auto-refetch).
    3. **hooks/use-flight-search.ts** — copy RESEARCH Pattern 3 verbatim. queryKey = `['flights', q.from, q.to, q.dep?.toISOString(), q.ret?.toISOString(), q.adt, q.chd, q.infl, q.infs, q.cabin]` (NO filters/sort in the key). `enabled: Boolean(q.from && q.to && q.dep)`. queryFn POSTs to `/api/search/flights` with the criteria.
    4. **components/search/airport-combobox.tsx** — cmdk `<Command>` inside Radix `<Popover>`. `useState<string>('')` for query; `useEffect` debounces 200ms → `fetch('/api/airports?q='+q, { signal: controller.signal })`; aborts previous. Render items as `"{iata} — {name}"`. Min 2 chars before query.
    5. **components/search/passenger-selector.tsx** — Popover with four counter rows. Labels exactly: "Adults", "Children (2–11)", "Infants on lap (<2)", "Infants in seat (<2)". Constraints: total ≤ 9; `infantsLap ≤ adults`; `infantsLap + infantsSeat ≤ 2 * adults` (airline-rule validation FLTB-01). Disable +/- at limits. Inline error text in `red-600` 12px when rule violated.
    6. **components/search/date-range-picker.tsx** — copy PATTERNS §date-range-picker pattern verbatim (Popover + react-day-picker Calendar mode="range", numberOfMonths=2, today min, today+361d max).
    7. **components/search/cabin-class-picker.tsx** — segmented pills using starterKit tabs/select primitives. Options: Economy / Premium economy / Business / First. Single selection.
    8. **components/search/flight-search-form.tsx** — RHF form with zod schema. Fields in UI-SPEC order. On submit, call `router.push('/flights/results?' + qs)` using `nuqs` URL serializer.
    9. **components/results/flight-card.tsx** — stacked card per UI-SPEC: airline logo (24px), name, departure time + IATA → arrow → arrival time + IATA, duration, stops, baggage icon with allowance, right-aligned all-in price with `incl. taxes` small label. Expandable section showing base/YQ-YR/tax rows. Selected state: left 4px `blue-600` border + ring-1.
    10. **components/results/filter-rail.tsx** — radio stops (any/0/1/2+), checkbox airline list (drawn from current results — client-side), 4 time-window chips, price slider. All filter state flows to nuqs params. Counts shown next to each option computed from cache (Pitfall 12).
    11. **components/results/sort-bar.tsx** — Select with Price / Duration / Departure. Updates `sort` nuqs param. Empty-state copy from UI-SPEC: "No flights match your filters. Clear filters?" with a `Change search` button.
    12. **components/results/search-results-panel.tsx** — Client Component. `const { data, isLoading } = useFlightSearch()`; apply filters + sort via `useMemo` (NOT refetch). Render FilterRail + SortBar + result cards + skeleton while `isLoading`.
    13. **app/flights/page.tsx** — RSC that renders `<FlightSearchForm />` (Client Component) + a hero. Public (no auth required).
    14. **app/flights/results/page.tsx** — RSC shell that reads searchParams (Pitfall 11 async), renders `<SearchResultsPanel>` wrapped in `<QueryClientProvider>` + nuqs `<NuqsAdapter>`.
    15. **app/flights/[offerId]/page.tsx** — fare-rule details drawer route (RSC). Reads `await params`, fetches offer details, renders fare rules (baggage, changes, cancellation). `Continue` button → `/checkout/details?offerId=…`.
    16. **app/api/search/flights/route.ts** — POST handler `gatewayFetch('/search/flights', { method: 'POST', body })`; streams JSON back.
    17. **app/page.tsx** — update landing: hero + `<FlightSearchForm mode="embedded" />` + `Search flights` CTA (UI-SPEC copy).
    Commit: `feat(04-02): B2C-03/04 flight search + results + URL state`.
  </action>
  <verify>
    <automated>cd src/portals/b2c-web && pnpm test -- --run tests/search && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'parseAsIsoDate' src/portals/b2c-web/lib/search-params.ts` returns ≥ 1.
    - `grep -c 'staleTime: 90_000' src/portals/b2c-web/lib/query-client.ts src/portals/b2c-web/hooks/use-flight-search.ts` returns ≥ 1 (Pitfall 11).
    - `grep -cE 'AbortController' src/portals/b2c-web/components/search/airport-combobox.tsx` returns 1 (Pitfall 10).
    - `grep -cE 'q\\.length\\s*>=\\s*2|q\\.length\\s*>\\s*1|length < 2' src/portals/b2c-web/components/search/airport-combobox.tsx` returns ≥ 1 (min 2 chars).
    - `grep -cE 'incl\\. taxes|incl.\\s*taxes' src/portals/b2c-web/components/results/flight-card.tsx` returns ≥ 1 (UI-SPEC copy).
    - `grep -cE 'Adults|Children \\(2–11\\)|Infants on lap' src/portals/b2c-web/components/search/passenger-selector.tsx` returns ≥ 3.
    - `grep -c 'No flights match your filters' src/portals/b2c-web/components/results/**/*.tsx` returns ≥ 1.
    - `grep -c 'useQueryStates(searchParsers)' src/portals/b2c-web/hooks/use-flight-search.ts` returns 1.
    - `pnpm test` green for `tests/search/**`.
    - No usage of `force-cache` on authenticated fetch anywhere in this task's files (anti-pattern check).
  </acceptance_criteria>
  <done>
    Search is fully functional with URL-state, client-side filter/sort, IATA typeahead, and UI-SPEC-compliant copy.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Checkout — details → payment (Stripe) → processing → success, with email-verify gate</name>
  <files>
    src/portals/b2c-web/app/checkout/layout.tsx,
    src/portals/b2c-web/app/checkout/details/page.tsx,
    src/portals/b2c-web/app/checkout/payment/page.tsx,
    src/portals/b2c-web/app/checkout/processing/page.tsx,
    src/portals/b2c-web/app/checkout/success/page.tsx,
    src/portals/b2c-web/components/checkout/stepper.tsx,
    src/portals/b2c-web/components/checkout/email-verify-gate.tsx,
    src/portals/b2c-web/components/checkout/payment-element-wrapper.tsx,
    src/portals/b2c-web/components/checkout/passenger-details-form.tsx,
    src/portals/b2c-web/lib/stripe.ts,
    src/portals/b2c-web/tests/checkout/payment-element-wrapper.test.tsx,
    src/portals/b2c-web/tests/checkout/email-verify-gate.test.tsx,
    src/portals/b2c-web/e2e/flight-booking.spec.ts,
    src/portals/b2c-web/e2e/flight-booking-mobile.spec.ts
  </files>
  <read_first>
    src/portals/b2c-web/lib/auth.ts,
    src/portals/b2c-web/middleware.ts,
    src/portals/b2c-web/lib/api-client.ts,
    ui/starterKit/components/ui/dialog.jsx,
    ui/starterKit/components/ui/stepper.jsx,
    .planning/phases/04-b2c-portal-customer-facing/04-UI-SPEC.md,
    .planning/phases/04-b2c-portal-customer-facing/04-RESEARCH.md
  </read_first>
  <behavior>
    - `payment-element-wrapper.test.tsx`: renders `<PaymentElement>` wrapped in `<Elements stripe={memoizedPromise} options={{clientSecret}}>`; on `onSubmit` calls `stripe.confirmPayment({ elements, confirmParams: { return_url } })`; return_url contains `/checkout/processing?basket=`; memoized `loadStripe` not re-invoked on re-render (Pitfall 5).
    - `email-verify-gate.test.tsx`: when `email_verified=false`, modal renders with heading "Verify your email first"; Resend button calls `/api/auth/resend-verification` via POST; when `email_verified=true`, modal does not render.
    - `flight-booking.spec.ts` (desktop chromium e2e): happy path — search → select → fill passenger details → fill Stripe test PAN `4242…` → land on /checkout/processing → after saga simulator emits Confirmed, land on /checkout/success with PNR shown. Uses Stripe CLI for webhook forwarding.
    - `flight-booking-mobile.spec.ts` (iPhone 12 viewport): counts DOM-visible step changes — MUST be exactly 5 screens before landing on success; confirmation screen not counted (B2C-05).
  </behavior>
  <action>
    **RED:** write the four test files; run; must fail. Commit: `test(04-02): checkout stepper + payment-element + verify gate + e2e`.

    **GREEN:**
    1. **lib/stripe.ts** — copy RESEARCH §Code Examples memoized `loadStripe`: `let _p; export const getStripe = () => (_p ??= loadStripe(process.env.NEXT_PUBLIC_STRIPE_PK!))`.
    2. **components/checkout/stepper.tsx** — 5-step indicator using starterKit Stepper. Labels exactly: "Search", "Results", "Select", "Details", "Payment". Current-step prop driven by route segment.
    3. **components/checkout/email-verify-gate.tsx** — Client Component. Props `{ email: string; verified: boolean }`. If `!verified`, render Radix `<Dialog open>` (not closable — no `X`). Body copy from UI-SPEC: "Verify your email first" heading + `We need to confirm your email before you can pay. We sent a link to {email}.` Two buttons: `Resend email` (POST /api/auth/resend-verification → success toast "Verification email sent to {email}. It may take a minute to arrive.") and `I have verified, refresh` (calls `signIn("keycloak", { redirect: false })` then reloads the page to pick up updated `email_verified` claim).
    4. **components/checkout/payment-element-wrapper.tsx** — Client Component. Props `{ amount: number; currency: string; bookingId: string; clientSecret: string }`. Internally uses `useStripe()`, `useElements()`. Button label `Pay £{formatMoney(amount,currency)}`. On click: disable button, show inline spinner (don't replace label), `await stripe.confirmPayment({ elements, confirmParams: { return_url: `${location.origin}/checkout/processing?booking=${bookingId}` } })`. On error surface via sonner toast ONLY if non-3DS; 3DS redirects away so no error path here.
    5. **components/checkout/passenger-details-form.tsx** — RHF+zod. For each passenger (derived from URL `adt/chd/infl/infs`): title, firstName, lastName, dob, passportNumber, passportExpiry, nationality. zod: future DOB rejected, passport exp > today+6mo. Submit → POST `/api/bookings` via fetch → navigate to `/checkout/payment?booking={id}`.
    6. **app/checkout/layout.tsx** — RSC: `const session = await auth(); if (!session) redirect('/login?callbackUrl=/checkout/details...')`. Renders `<Stepper currentStep={segment} />` + children.
    7. **app/checkout/details/page.tsx** — Client Component (RHF). Reads `offerId` from searchParams.
    8. **app/checkout/payment/page.tsx** — **RSC**. Read `session` via `auth()`; if `!session.email_verified` mount `<EmailVerifyGate email={session.user.email} verified={false} />` AND DO NOT mount `<Elements>` (Pitfall 5 — avoid shipping Stripe iframe unnecessarily). If verified: `const res = await gatewayFetch(\`/bookings/\${bookingId}/payment-intent\`, { method: 'POST' }); const { client_secret, amount, currency } = await res.json(); return <Elements stripe={getStripe()} options={{ clientSecret: client_secret }}><PaymentElementWrapper ... /></Elements>`.
    9. **app/checkout/processing/page.tsx** — Client Component (uses state + polling). Reads `booking` from searchParams. Polls `GET /api/bookings/{id}/status` every 2000ms. Terminal states: `Confirmed` → `router.push('/checkout/success?booking={id}')`; `Failed` → render `<PartialFailureBanner>` or specific failure copy from UI-SPEC; `Cancelled` → failure copy. 90-second hard cap: render "Taking longer than expected — we'll email you" + link to dashboard (UI-SPEC copy Pitfall 6).
    10. **app/checkout/success/page.tsx** — RSC. Fetches booking detail; renders success headline from UI-SPEC (`Flight booked. Booking reference: {PNR}. Your e-ticket will arrive by email within 60 seconds.`) + `Download receipt` CTA.
    11. **Middleware update:** already covers `/checkout/:path*` redirect to /login when unauthenticated. Add an ADDITIONAL rule in `middleware.ts` that, when path starts with `/checkout/payment` AND `!session.email_verified`, redirect to `/checkout/payment?verify=1` — Client gate in page re-reads this and forces the modal open (belt-and-braces; page already handles it but middleware catches direct navigation).
    Commit: `feat(04-02): B2C-05/06 checkout + Stripe PaymentElement + verify gate + processing`.
  </action>
  <verify>
    <automated>cd src/portals/b2c-web && pnpm test -- --run tests/checkout && pnpm typecheck && pnpm exec playwright test --grep "flight-booking" --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE 'getStripe\\(\\)|loadStripe' src/portals/b2c-web/lib/stripe.ts` returns ≥ 1.
    - `grep -c '_p ??= loadStripe' src/portals/b2c-web/lib/stripe.ts` returns 1 (memoized — Pitfall 5).
    - `grep -c 'return_url.*checkout/processing' src/portals/b2c-web/components/checkout/payment-element-wrapper.tsx` returns 1.
    - `grep -c 'Verify your email first' src/portals/b2c-web/components/checkout/email-verify-gate.tsx` returns 1 (UI-SPEC copy).
    - `grep -cE '/api/auth/resend-verification' src/portals/b2c-web/components/checkout/email-verify-gate.tsx` returns 1 (reuses 04-01 route).
    - `grep -c 'bookings/.*status' src/portals/b2c-web/app/checkout/processing/page.tsx` returns ≥ 1.
    - `grep -cE '2000|2_000' src/portals/b2c-web/app/checkout/processing/page.tsx` returns ≥ 1 (2s poll).
    - `grep -cE 'Taking longer than expected' src/portals/b2c-web/app/checkout/processing/page.tsx` returns 1 (UI-SPEC 90s cap).
    - `grep -rE '<Elements ' src/portals/b2c-web/app/` returns 1 ONLY under `app/checkout/payment/page.tsx` (Pitfall 5 — Elements not in root layout).
    - `grep -c 'email_verified' src/portals/b2c-web/app/checkout/payment/page.tsx` returns ≥ 1.
    - `pnpm test` green for `tests/checkout/**`.
    - Playwright mobile spec passes and asserts 5 distinct step screens (B2C-05).
    - No test or component treats `return_url` arrival as success (`grep -rE 'success' src/portals/b2c-web/app/checkout/` never appears on a return_url redirect — only post-poll — Pitfall 6).
  </acceptance_criteria>
  <done>
    A flight booking completes end-to-end on desktop chromium and iPhone 12 viewport. Email gate blocks unverified users at payment. Stripe PaymentElement is only loaded on the payment step. Success is driven by webhook-confirmed saga state, not by the client-side Stripe return.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Stripe iframe | Card data goes DIRECTLY to Stripe (SAQ-A, COMP-01) |
| Browser → /api/search/flights | Public; rate-limited at gateway |
| Browser → /api/airports | Public; rate-limited (60/min IP) |
| Next.js → PaymentService /bookings/{id}/payment-intent | Authenticated; idempotency-keyed server-side |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-02-01 | I (Card-data leak) | payment-element-wrapper | mitigate | PaymentElement is a Stripe iframe; portal never touches card fields; CSP from 04-00 enforces Stripe-only frame-src (COMP-01) |
| T-04-02-02 | S (False success — return_url treated as confirmed) | checkout/processing | mitigate | Poll-based terminal state via /bookings/{id}/status; 90s cap; client-side return_url is ignored as a success signal (Pitfall 6, FLTB/PAY D-12) |
| T-04-02-03 | T (Unverified-email bypass) | checkout/payment | mitigate | Server-side `auth()` on RSC + middleware double-check; PaymentElement NOT mounted when unverified |
| T-04-02-04 | D (IATA endpoint abuse) | AirportsController | mitigate | Fixed-window rate-limit 60/min/IP; min q length 2; max length 8 |
| T-04-02-05 | T (Stale offer price at book time) | flow details → payment | mitigate | Phase 3 saga's `PriceReconfirmed` step handles re-price; UI shows "Price changed" dialog if delta > 0 (UI-SPEC copy) |
| T-04-02-06 | I (Passport data in logs) | passenger-details-form → POST /bookings | mitigate | Inherits Phase 3 OpenTelemetry SensitiveAttributeProcessor; passport encrypted at rest AES-256 (COMP-02) |
| T-04-02-07 | E (Double-booking via double-click) | "Continue" + "Pay" buttons | mitigate | Disable on submit; saga idempotency keys per booking (Phase 3 D-13) |
| T-04-02-08 | R (Stripe return from 3DS dropping cookie) | checkout/processing | mitigate | SameSite=Lax (default) survives top-level cross-site GET (Pitfall 18) |
| T-04-02-09 | S (Open redirect on callbackUrl) | login/signIn flow from checkout | mitigate | Same-origin check already enforced in 04-01; Auth.js v5 validates by default |
</threat_model>

<verification>
- `pnpm test` green for tests/search/** and tests/checkout/**.
- `pnpm exec playwright test --grep "flight-booking" --project=chromium` green.
- `pnpm exec playwright test --grep "flight-booking" --project=mobile` green and asserts 5-step flow.
- Manual UAT (VALIDATION.md): 60s email SLA (real inbox), mobile 5-step verification, full search-to-success on iPhone viewport.
</verification>

<success_criteria>
- B2C-03 (search form, IATA autocomplete, date picker, passenger selector) complete.
- B2C-04 (sorted results with client-side filters) complete.
- B2C-05 (mobile ≤5 steps) verified by Playwright mobile spec.
- B2C-06 (Stripe Elements SAQ-A) complete; CSP verified.
- FLTB-01/02/03 consumed correctly (multi-pax, fare rules, all-in pricing with YQ/YR separated).
</success_criteria>

<output>
After completion, create `.planning/phases/04-b2c-portal-customer-facing/04-02-SUMMARY.md` with: IATA dataset version + row count, Stripe test-PAN smoke notes, any deviations from Pitfall mitigations, mobile 5-step walkthrough screenshots path.
</output>
