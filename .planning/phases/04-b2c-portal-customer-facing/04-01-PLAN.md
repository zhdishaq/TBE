---
phase: 04-b2c-portal-customer-facing
plan: 01
type: execute
wave: 2
depends_on: ["04-00"]
files_modified:
  - src/portals/b2c-web/app/(public)/login/page.tsx
  - src/portals/b2c-web/app/(public)/register/page.tsx
  - src/portals/b2c-web/app/(public)/verify-email/page.tsx
  - src/portals/b2c-web/app/(public)/password-reset/page.tsx
  - src/portals/b2c-web/app/bookings/page.tsx
  - src/portals/b2c-web/app/bookings/[id]/page.tsx
  - src/portals/b2c-web/app/api/bookings/[id]/receipt.pdf/route.ts
  - src/portals/b2c-web/app/api/auth/resend-verification/route.ts
  - src/portals/b2c-web/components/account/booking-row.tsx
  - src/portals/b2c-web/components/account/dashboard-tabs.tsx
  - src/portals/b2c-web/components/account/empty-state.tsx
  - src/portals/b2c-web/lib/keycloak-admin.ts
  - src/portals/b2c-web/lib/formatters.ts
  - src/portals/b2c-web/tests/account/dashboard-tabs.test.tsx
  - src/portals/b2c-web/tests/account/empty-state.test.tsx
  - src/portals/b2c-web/e2e/register-login.spec.ts
  - src/portals/b2c-web/e2e/password-reset.spec.ts
  - src/portals/b2c-web/e2e/receipt-download.spec.ts
  - src/services/BookingService/BookingService.API/Controllers/ReceiptsController.cs
  - src/services/BookingService/BookingService.Application/Pdf/IBookingReceiptPdfGenerator.cs
  - src/services/BookingService/BookingService.Infrastructure/Pdf/QuestPdfBookingReceiptGenerator.cs
  - src/services/BookingService/BookingService.API/Program.cs
  - tests/BookingService.Tests/ReceiptsControllerTests.cs
  - tests/BookingService.Tests/QuestPdfBookingReceiptGeneratorTests.cs
autonomous: true
requirements: [B2C-01, B2C-02, B2C-07, B2C-08, NOTF-02]  # NOTF-02 partially — resend-verification email path is Keycloak-driven

user_setup:
  - service: keycloak
    why: "Resend-verification endpoint calls Keycloak Admin API (Pitfall 8); depends on tbe-b2c-admin service client provisioned in 04-00."
    env_vars:
      - name: KEYCLOAK_B2C_ADMIN_CLIENT_ID
        source: "Keycloak admin console (provisioned in 04-00)"
      - name: KEYCLOAK_B2C_ADMIN_CLIENT_SECRET
        source: "Keycloak admin console → Clients → tbe-b2c-admin → Credentials"

must_haves:
  truths:
    - "A new user can reach /login, click 'Sign in with Keycloak', register via Keycloak's flow, verify their email via the emailed link, and return to the portal with an authenticated session"
    - "An authenticated-but-unverified user sees the dashboard in read-only mode AND the checkout gate (from later plans) blocks them at payment; clicking 'Resend verification email' on the gate triggers a real Keycloak admin API call"
    - "A logged-in user can reach /bookings, see their upcoming and past bookings from GET /customers/me/bookings, and click 'Download receipt' to get a PDF"
    - "The receipt PDF contains PNR, ticket number, fare breakdown (base / YQ-YR / taxes per FLTB-03), and all-in total"
    - "A non-owner user receives 403 on GET /bookings/{id}/receipt.pdf (ownership check enforced)"
    - "The portal honours Auth.js session for /bookings/** — unauthenticated requests are redirected to /login?callbackUrl=…"
  artifacts:
    - path: "src/portals/b2c-web/app/(public)/login/page.tsx"
      provides: "Sign-in page calling signIn('keycloak')"
    - path: "src/portals/b2c-web/app/bookings/page.tsx"
      provides: "Dashboard RSC reading GET /customers/me/bookings with Upcoming/Past tabs (D-17)"
    - path: "src/portals/b2c-web/app/bookings/[id]/page.tsx"
      provides: "Booking detail with Download receipt link"
    - path: "src/portals/b2c-web/app/api/bookings/[id]/receipt.pdf/route.ts"
      provides: "Streaming pass-through to gateway (Pitfall 14)"
    - path: "src/portals/b2c-web/app/api/auth/resend-verification/route.ts"
      provides: "Route handler calling Keycloak admin send-verify-email (Pitfall 8)"
    - path: "src/portals/b2c-web/lib/keycloak-admin.ts"
      provides: "Client-credentials token fetch + send-verify-email caller"
    - path: "src/services/BookingService/BookingService.API/Controllers/ReceiptsController.cs"
      provides: "[Authorize] GET /bookings/{id}/receipt.pdf with ownership check"
      exports: ["GetReceiptAsync"]
    - path: "src/services/BookingService/BookingService.Infrastructure/Pdf/QuestPdfBookingReceiptGenerator.cs"
      provides: "Server-side receipt PDF with fare breakdown (D-15, FLTB-03)"
  key_links:
    - from: "src/portals/b2c-web/app/bookings/page.tsx"
      to: "GET /customers/me/bookings via gatewayFetch"
      via: "gatewayFetch('/customers/me/bookings')"
      pattern: "gatewayFetch\\(['\"]/customers/me/bookings['\"]"
    - from: "src/portals/b2c-web/app/api/bookings/[id]/receipt.pdf/route.ts"
      to: "BookingService /bookings/{id}/receipt.pdf"
      via: "gatewayFetch passes Bearer server-side; body streamed"
      pattern: "new Response\\(upstream\\.body"
    - from: "src/services/BookingService/BookingService.API/Controllers/ReceiptsController.cs"
      to: "IBookingReceiptPdfGenerator (QuestPDF impl)"
      via: "constructor DI + pdfGen.GenerateAsync(booking, ct)"
      pattern: "pdfGen\\.GenerateAsync"
    - from: "src/portals/b2c-web/app/api/auth/resend-verification/route.ts"
      to: "Keycloak Admin API /users/{id}/send-verify-email"
      via: "client_credentials → bearer → PUT"
      pattern: "send-verify-email"
---

<objective>
Deliver B2C account surfaces: login/register/password-reset/verify-email routes (all thin wrappers over Keycloak flows per D-04, D-06), a customer dashboard RSC reading `GET /customers/me/bookings` (D-17) with Upcoming/Past tabs, a booking detail page with a working "Download receipt" link, and the BookingService `ReceiptsController` + QuestPDF generator (D-15) that serves it. Resend-verification goes through a Next.js route handler calling Keycloak Admin API with the `tbe-b2c-admin` service client provisioned in 04-00 (Pitfall 8).

Purpose: B2C-01, B2C-02, B2C-07, B2C-08 — the skeleton the user lives in once booking plans (04-02..04-04) ship.
Output: Working account surfaces + receipt PDF endpoint + resend-verification path.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-b2c-portal-customer-facing/04-CONTEXT.md
@.planning/phases/04-b2c-portal-customer-facing/04-RESEARCH.md
@.planning/phases/04-b2c-portal-customer-facing/04-PATTERNS.md
@.planning/phases/04-b2c-portal-customer-facing/04-UI-SPEC.md
@.planning/phases/04-b2c-portal-customer-facing/04-00-PLAN.md
@src/services/BookingService/BookingService.API/Controllers/BookingsController.cs
@src/services/NotificationService/NotificationService.Infrastructure/Pdf/QuestPdfETicketGenerator.cs
@src/services/BookingService/BookingService.API/Program.cs

<interfaces>
From Phase 3 (already shipped):
- `BookingsController.GetBooking(Guid id)` — returns BookingDtoPublic
- `BookingsController.GetCustomerBookings(string customerId)` — NOTE: current route is `/customers/{customerId}/bookings`; this plan ADDS a convenience route `GET /customers/me/bookings` resolving customerId from JWT sub (D-17 simplification). Alternative: the portal always calls `/customers/{sub}/bookings` and no new route is needed.
- `BookingSagaState { CorrelationId, UserId, BookingReference, ProductType, TotalAmount, Currency, GdsPnr?, TicketNumber?, PassengerCount?, FareBreakdown (to be added in field below) }`

Pattern analog for QuestPDF: src/services/NotificationService/NotificationService.Infrastructure/Pdf/QuestPdfETicketGenerator.cs — static constructor sets LicenseType.Community; Document.Create with page margin 30; Header + Content + Footer.

Auth.js v5 server helpers available from 04-00:
- `auth()` → Session | null with { access_token, email_verified, expires_at }
- `signIn("keycloak")`, `signOut()`
- `gatewayFetch(path, init?)` from lib/api-client (forwards Bearer)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Backend — ReceiptsController + QuestPdfBookingReceiptGenerator + "me" bookings route</name>
  <files>
    src/services/BookingService/BookingService.API/Controllers/ReceiptsController.cs,
    src/services/BookingService/BookingService.Application/Pdf/IBookingReceiptPdfGenerator.cs,
    src/services/BookingService/BookingService.Infrastructure/Pdf/QuestPdfBookingReceiptGenerator.cs,
    src/services/BookingService/BookingService.API/Program.cs,
    src/services/BookingService/BookingService.API/Controllers/BookingsController.cs,
    tests/BookingService.Tests/ReceiptsControllerTests.cs,
    tests/BookingService.Tests/QuestPdfBookingReceiptGeneratorTests.cs
  </files>
  <read_first>
    src/services/BookingService/BookingService.API/Controllers/BookingsController.cs,
    src/services/NotificationService/NotificationService.Infrastructure/Pdf/QuestPdfETicketGenerator.cs,
    src/services/BookingService/BookingService.API/Program.cs,
    tests/BookingService.Tests/ReceiptsControllerTests.cs,
    tests/BookingService.Tests/QuestPdfBookingReceiptGeneratorTests.cs
  </read_first>
  <behavior>
    - GetReceipt returns `FileContentResult` with content-type `application/pdf` + `content-disposition` attachment for a booking whose UserId equals JWT `sub`.
    - GetReceipt returns `Forbid()` for another user's booking unless the caller is in role `backoffice-staff`.
    - GetReceipt returns `NotFound()` when no saga state matches CorrelationId.
    - QuestPdfBookingReceiptGenerator produces non-empty byte[] with header containing `BookingReference`, content containing `PNR`, `Ticket`, `Base fare`, `YQ/YR`, `Taxes`, `Total` (FLTB-03 — pricing separated, not merged).
    - `/customers/me/bookings` resolves `customerId` from `ClaimTypes.NameIdentifier ?? "sub"` and returns the same DTO shape as the existing explicit-id endpoint.
  </behavior>
  <action>
    **RED phase:** Replace the `Assert.Fail("Red placeholder …")` bodies in the two test files from 04-00 with real assertions driving the behavior above. Add `BookingDbContext` in-memory or use MsSqlContainerFixture; seed a `BookingSagaState` with known UserId, PNR, TicketNumber, and a fare breakdown (base=100, yqyr=30, taxes=20, total=150, currency=GBP). Use NSubstitute for `IBookingReceiptPdfGenerator` in the controller ownership tests; use a real generator in the PDF generator tests. Run tests; MUST fail. Commit: `test(04-01): ReceiptsController ownership + QuestPdfBookingReceiptGenerator`.

    **GREEN phase:**
    1. **Extend `BookingSagaState`** with three persisted decimals: `BaseFareAmount`, `SurchargeAmount` (YQ/YR), `TaxAmount`. Add migration `20260500000000_AddReceiptFareBreakdown.cs` hand-authored (ModelSnapshot unusable per 03-01 deviation #2): ALTER TABLE Saga.BookingSagaState ADD three `decimal(18,4) NOT NULL DEFAULT 0`. Update `BookingSagaStateMap.cs` with `HasColumnType("decimal(18,4)")`.
    2. **IBookingReceiptPdfGenerator.cs** (Application layer):
       ```csharp
       namespace TBE.BookingService.Application.Pdf;
       public interface IBookingReceiptPdfGenerator {
           Task<byte[]> GenerateAsync(BookingSagaState state, CancellationToken ct);
       }
       ```
    3. **QuestPdfBookingReceiptGenerator.cs** (Infrastructure): copy the structure of `QuestPdfETicketGenerator.cs` verbatim per PATTERNS (static ctor with `LicenseType.Community` + TODO(prod) comment; `Document.Create(c => c.Page(p => { p.Margin(30); p.Header().Text($"Receipt — {state.BookingReference}"); p.Content().Column(col => { col.Item().Text($"PNR: {state.GdsPnr}"); col.Item().Text($"Ticket: {state.TicketNumber}"); col.Item().LineHorizontal(1); col.Item().Text($"Base fare: {state.Currency} {state.BaseFareAmount:N2}"); col.Item().Text($"YQ/YR surcharges: {state.Currency} {state.SurchargeAmount:N2}"); col.Item().Text($"Taxes: {state.Currency} {state.TaxAmount:N2}"); col.Item().LineHorizontal(1); col.Item().Text($"Total: {state.Currency} {state.TotalAmount:N2}").SemiBold(); }); p.Footer()…; }));` then `.GeneratePdf()`.
    4. **ReceiptsController.cs** — copy PATTERNS §ReceiptsController code verbatim: `[ApiController, Route("bookings"), Authorize]`, primary ctor `(BookingDbContext db, IBookingReceiptPdfGenerator pdfGen, ILogger<ReceiptsController> logger)`. `GetReceipt(Guid id, CancellationToken ct)`: resolve userId from `NameIdentifier ?? sub`; `AsNoTracking().FirstOrDefaultAsync(s => s.CorrelationId == id, ct)`; `NotFound()` if null; `Forbid()` if `booking.UserId != userId && !User.IsInRole("backoffice-staff")`; else `return File(await pdfGen.GenerateAsync(booking, ct), "application/pdf", $"receipt-{booking.BookingReference}.pdf")`.
    5. **Program.cs** — add `builder.Services.AddScoped<IBookingReceiptPdfGenerator, QuestPdfBookingReceiptGenerator>();` near the existing service registrations (around line 77 per PATTERNS).
    6. **BookingsController `/me/` route** — add `[HttpGet("/customers/me/bookings")]` returning the same projection as `GetCustomerBookings(customerId)` with customerId resolved from `NameIdentifier ?? sub`. Reuse existing pagination params (`skip`, `take`).
    Commit: `feat(04-01): B2C-08 receipt endpoint + QuestPDF generator + /customers/me/bookings`.

    **Refactor (optional):** consolidate fare-breakdown math into a readonly record if it's referenced from ≥2 files.
  </action>
  <verify>
    <automated>dotnet test tests/BookingService.Tests/BookingService.Tests.csproj --filter "FullyQualifiedName~Receipt|FullyQualifiedName~QuestPdfBookingReceiptGenerator" -warnaserror</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'class ReceiptsController' src/services/BookingService/BookingService.API/Controllers/ReceiptsController.cs` returns 1.
    - `grep -c '\\[Authorize\\]' src/services/BookingService/BookingService.API/Controllers/ReceiptsController.cs` returns ≥ 1 (COMP-04).
    - `grep -cE 'booking\\.UserId\\s*!=\\s*userId\\s*&&\\s*!User\\.IsInRole' src/services/BookingService/BookingService.API/Controllers/ReceiptsController.cs` returns 1 (ownership, mirrors BookingsController).
    - `grep -c 'LicenseType.Community' src/services/BookingService/BookingService.Infrastructure/Pdf/QuestPdfBookingReceiptGenerator.cs` returns 1.
    - `grep -cE 'TODO\\(prod\\)' src/services/BookingService/BookingService.Infrastructure/Pdf/QuestPdfBookingReceiptGenerator.cs` returns ≥ 1 (license upgrade reminder).
    - `grep -cE 'YQ|Surcharge' src/services/BookingService/BookingService.Infrastructure/Pdf/QuestPdfBookingReceiptGenerator.cs` returns ≥ 1 (FLTB-03 separation).
    - `grep -cE 'IBookingReceiptPdfGenerator,\\s*QuestPdfBookingReceiptGenerator' src/services/BookingService/BookingService.API/Program.cs` returns 1.
    - `grep -cE 'HttpGet\\(\"/customers/me/bookings\"\\)|HttpGet\\(\"customers/me/bookings\"\\)' src/services/BookingService/BookingService.API/Controllers/BookingsController.cs` returns 1.
    - `dotnet test` target passes, no red placeholders remain in the two receipt test files.
  </acceptance_criteria>
  <done>
    Red-phase tests from 04-00 are green. Receipt endpoint enforces ownership, returns streaming PDF with fare breakdown, and the `/me` bookings route responds with the caller's bookings.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Frontend auth pages + resend-verification route + dashboard + receipt download</name>
  <files>
    src/portals/b2c-web/app/(public)/login/page.tsx,
    src/portals/b2c-web/app/(public)/register/page.tsx,
    src/portals/b2c-web/app/(public)/verify-email/page.tsx,
    src/portals/b2c-web/app/(public)/password-reset/page.tsx,
    src/portals/b2c-web/app/bookings/page.tsx,
    src/portals/b2c-web/app/bookings/[id]/page.tsx,
    src/portals/b2c-web/app/api/bookings/[id]/receipt.pdf/route.ts,
    src/portals/b2c-web/app/api/auth/resend-verification/route.ts,
    src/portals/b2c-web/lib/keycloak-admin.ts,
    src/portals/b2c-web/lib/formatters.ts,
    src/portals/b2c-web/components/account/booking-row.tsx,
    src/portals/b2c-web/components/account/dashboard-tabs.tsx,
    src/portals/b2c-web/components/account/empty-state.tsx,
    src/portals/b2c-web/tests/account/dashboard-tabs.test.tsx,
    src/portals/b2c-web/tests/account/empty-state.test.tsx,
    src/portals/b2c-web/e2e/register-login.spec.ts,
    src/portals/b2c-web/e2e/password-reset.spec.ts,
    src/portals/b2c-web/e2e/receipt-download.spec.ts
  </files>
  <read_first>
    src/portals/b2c-web/lib/auth.ts,
    src/portals/b2c-web/lib/api-client.ts,
    src/portals/b2c-web/middleware.ts,
    ui/starterKit/app/(layouts)/layout-1/page.jsx,
    ui/starterKit/components/ui/tabs.jsx,
    ui/starterKit/components/ui/card.jsx,
    .planning/phases/04-b2c-portal-customer-facing/04-UI-SPEC.md
  </read_first>
  <behavior>
    - `/login` renders a sign-in button that invokes `signIn("keycloak", { callbackUrl })`; `callbackUrl` is read from searchParams and same-origin validated (anti-open-redirect).
    - `/register` redirects to Keycloak's registration page (`{issuer}/protocol/openid-connect/registrations?client_id=…&response_type=code…`).
    - `/password-reset` redirects to Keycloak's forgot-password flow.
    - `/verify-email` landing page after Keycloak verify-email link: displays success copy and a `Continue` button that forces a session refresh.
    - `/bookings` RSC fetches `GET /customers/me/bookings`, splits into Upcoming vs Past by comparing departure/checkout date to `new Date()`, renders `<DashboardTabs>` with badge counts.
    - `/bookings/[id]` RSC fetches the single booking and shows itinerary + `Download receipt` anchor to `/api/bookings/{id}/receipt.pdf`.
    - `/api/bookings/[id]/receipt.pdf` streams upstream body through (Pitfall 14 — no buffering).
    - `/api/auth/resend-verification` (POST) reads the current session's `sub`, acquires a service-account token via `client_credentials` grant against `tbe-b2c-admin`, then calls `PUT {issuer.replace('/realms/tbe-b2c','')}/admin/realms/tbe-b2c/users/{sub}/send-verify-email`. Returns 202 on success, 4xx on token/api failure.
    - Copy strings match UI-SPEC §Copywriting Contract verbatim (no paraphrasing): "No upcoming trips" / "Your booking history will show here once you have completed a trip." / etc.
  </behavior>
  <action>
    **RED:** Write `tests/account/dashboard-tabs.test.tsx` with cases: (a) renders Upcoming tab default active, (b) renders empty-state copy exactly "No upcoming trips" and body "When you book a flight, hotel, or car, it will appear here. Search now to get started.", (c) partitions bookings into Upcoming vs Past using `departureDate > now`. Write `tests/account/empty-state.test.tsx` for each surface defined in UI-SPEC. Write `e2e/register-login.spec.ts` using the `authedPage` fixture from 04-00: completes the sign-in round-trip, asserts `/bookings` shows empty state. Write `e2e/password-reset.spec.ts`: clicks "Forgot password?" from `/login`, asserts redirect URL starts with `{issuer}/login-actions/reset-credentials`. Write `e2e/receipt-download.spec.ts`: using a test booking seeded via a test-mode API (fixture), asserts response status 200 and content-type `application/pdf` on `/api/bookings/{id}/receipt.pdf`. Run; MUST fail. Commit: `test(04-01): account dashboard + auth round-trip + receipt download`.

    **GREEN:**
    1. **app/(public)/login/page.tsx** — Client Component: `<Button onClick={() => signIn("keycloak", { callbackUrl: validatedCallback })}>Sign in with Keycloak</Button>` + a `<Link href="/password-reset">Forgot password?</Link>`. Validate `callbackUrl` is same-origin before passing (anti-open-redirect, ASVS V4).
    2. **app/(public)/register/page.tsx** — RSC that builds the Keycloak registration URL from env and `redirect()`s. No client JS.
    3. **app/(public)/password-reset/page.tsx** — RSC redirect to `{issuer}/login-actions/reset-credentials?client_id={KEYCLOAK_B2C_CLIENT_ID}`.
    4. **app/(public)/verify-email/page.tsx** — static copy "Email verified." + button `onClick={() => signIn("keycloak", { callbackUrl: "/bookings" })}` (forces token refresh via re-auth, simpler than `update()` gymnastics).
    5. **lib/keycloak-admin.ts** — implement `getServiceAccountToken()` (POST `{issuer}/protocol/openid-connect/token` with `client_credentials`) + `sendVerifyEmail(userSub)` (PUT `admin/realms/tbe-b2c/users/{sub}/send-verify-email` with Bearer). Cache the token in memory with 30s skew; refresh when expired. NEVER log the token.
    6. **app/api/auth/resend-verification/route.ts** — Node route handler (NOT edge): `export async function POST()`: `const session = await auth(); if (!session?.user?.id) return new Response(null, {status:401}); await sendVerifyEmail(session.user.id); return new Response(null, {status:202});`.
    7. **lib/formatters.ts** — implement `formatMoney(amount: number, currency: string) = new Intl.NumberFormat('en-GB', { style: 'currency', currency }).format(amount)` (Pitfall 13 — no hard-coded £), `formatDate(iso: string) = ...`, `formatDuration(minutes: number) = ...`.
    8. **components/account/empty-state.tsx** — props `{ heading: string; body: string; action?: { href: string; label: string } }`. Render `<div>` with UI-SPEC spacing (card padding md, heading role/Display size, body role/Body).
    9. **components/account/booking-row.tsx** — props `{ booking: BookingDtoPublic }`. Render product icon (lucide `Plane`/`Bed`/`Car` by `productType`), reference + dates + status badge (status color map from UI-SPEC), chevron right. `Link` to `/bookings/{id}`.
    10. **components/account/dashboard-tabs.tsx** — Client Component using starterKit `Tabs`. Two TabsTriggers: `Upcoming ({upcoming.length})` + `Past ({past.length})`. TabsContent renders BookingRow list or `<EmptyState heading="No upcoming trips" body="When you book a flight, hotel, or car, it will appear here. Search now to get started." action={{href: "/", label: "Start a search"}} />`. Exact copy from UI-SPEC.
    11. **app/bookings/page.tsx** — `export const dynamic = "force-dynamic";` (Pitfall 15). `const session = await auth(); if (!session) redirect("/login?callbackUrl=/bookings");` then `const res = await gatewayFetch("/customers/me/bookings"); const bookings = await res.json();`. Partition by `new Date(booking.departureDate) > new Date()` → upcoming (ascending by date) / past (descending). `<DashboardTabs upcoming={upcoming} past={past} />`.
    12. **app/bookings/[id]/page.tsx** — `async function Page({ params }: { params: Promise<{id:string}> })`; `const { id } = await params;` (Pitfall 11). Fetch booking; render itinerary + `<a href={"/api/bookings/"+id+"/receipt.pdf"} download><Button>Download receipt</Button></a>`.
    13. **app/api/bookings/[id]/receipt.pdf/route.ts** — copy PATTERNS §Frontend receipt pass-through verbatim: `params` is Promise (Pitfall 11); `gatewayFetch` upstream; `new Response(upstream.body, { headers: { 'content-type': 'application/pdf', 'content-disposition': upstream.headers.get('content-disposition') ?? \`attachment; filename=\"receipt-${id}.pdf\"\` } })` — Pitfall 14 stream-through.
    Commit: `feat(04-01): B2C-01/02/07/08 account surfaces + receipt download + resend-verification`.
  </action>
  <verify>
    <automated>cd src/portals/b2c-web && pnpm test -- --run tests/account && pnpm typecheck && pnpm exec playwright test --grep "receipt-download|password-reset|register-login" --project=chromium</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rE 'signIn\\([\"'\\'']keycloak[\"'\\'']' src/portals/b2c-web/app/\\(public\\)/login/page.tsx` returns ≥ 1.
    - `grep -c 'No upcoming trips' src/portals/b2c-web/components/account/dashboard-tabs.tsx src/portals/b2c-web/components/account/empty-state.tsx` returns ≥ 1 (UI-SPEC copy).
    - `grep -c 'Your booking history will show here once you have completed a trip' src/portals/b2c-web/components/account/**/*.tsx` returns ≥ 1.
    - `grep -c "export const dynamic = ['\"]force-dynamic['\"]" src/portals/b2c-web/app/bookings/page.tsx` returns 1 (Pitfall 15).
    - `grep -c 'await params' src/portals/b2c-web/app/bookings/\\[id\\]/page.tsx` returns ≥ 1 (Pitfall 11).
    - `grep -c 'new Response(upstream.body' src/portals/b2c-web/app/api/bookings/\\[id\\]/receipt.pdf/route.ts` returns 1 (Pitfall 14 — stream through).
    - `grep -c 'send-verify-email' src/portals/b2c-web/lib/keycloak-admin.ts` returns 1 (Pitfall 8).
    - `grep -c 'client_credentials' src/portals/b2c-web/lib/keycloak-admin.ts` returns 1.
    - `grep -rE 'Intl\\.NumberFormat' src/portals/b2c-web/lib/formatters.ts` returns ≥ 1 (Pitfall 13 — no hard-coded £).
    - `pnpm test` green for `tests/account/**`.
    - Playwright `receipt-download.spec.ts` passes against a mock gateway (fixture provides signed upstream response).
    - No file in `src/portals/b2c-web/app/**` imports `next-auth/react` inside a Server Component (use `auth()` — anti-pattern from RESEARCH).
  </acceptance_criteria>
  <done>
    Users can sign in, sign out, reset their password, receive verify-email, see their bookings, and download a receipt PDF. Resend-verification hits a real Keycloak admin endpoint via the service-account client.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js `/api/bookings/{id}/receipt.pdf` | Session cookie; stream-through |
| Next.js → BookingService `/bookings/{id}/receipt.pdf` | Bearer; ownership check server-side |
| Next.js → Keycloak Admin API | client_credentials Bearer from tbe-b2c-admin |
| Browser → `/api/auth/resend-verification` | Session cookie only; no user input payload |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01-01 | I (IDOR on receipt) | ReceiptsController.GetReceipt | mitigate | `booking.UserId != userId && !User.IsInRole("backoffice-staff")` check; covered by `GetReceipt_returns_403_for_other_user` test |
| T-04-01-02 | T (Tampering — receipt content) | QuestPdfBookingReceiptGenerator | accept | PDF generated server-side from persisted saga state; no client-supplied fields used |
| T-04-01-03 | S (Open redirect on signIn callbackUrl) | app/(public)/login/page.tsx | mitigate | Same-origin check before `signIn(..., { callbackUrl })`; Auth.js v5 also validates by default |
| T-04-01-04 | E (Elevation via Keycloak admin API) | lib/keycloak-admin.ts | mitigate | Dedicated tbe-b2c-admin service client scoped to `manage-users` only; token never exposed to browser; route handler is Node runtime only |
| T-04-01-05 | I (Session token leak via client JS) | bookings pages | mitigate | All API calls go through server-side `auth()` + `gatewayFetch`; no `access_token` prop passed to Client Components |
| T-04-01-06 | D (Stale dashboard after new booking) | app/bookings/page.tsx | mitigate | `export const dynamic = "force-dynamic"` (Pitfall 15) |
| T-04-01-07 | I (Large PDF buffered in memory) | /api/bookings/[id]/receipt.pdf | mitigate | Stream-through `new Response(upstream.body, …)` — Pitfall 14 |
| T-04-01-08 | T (Hardcoded currency) | money rendering | mitigate | `formatMoney(amount, currency)` helper; backend supplies currency (Pitfall 13) |
| T-04-01-09 | R (Verify-email token brute-force) | resend-verification route | accept | Rate-limiting inherited from Keycloak; not re-implemented in route handler |
</threat_model>

<verification>
- `dotnet test tests/BookingService.Tests` green with 6+ tests around receipts.
- `pnpm test` green for `tests/account/**`.
- Playwright e2e: `register-login`, `password-reset`, `receipt-download` pass. The Keycloak-backed tests self-skip if `TEST_KC_USER` unset.
- Manual UAT (VALIDATION.md): download PDF → visual check of fare breakdown; register a new user end-to-end in test Keycloak.
</verification>

<success_criteria>
- B2C-01/02/07/08 acceptance criteria (REQUIREMENTS.md) all hit:
  - Registration + verify-email flows complete via Keycloak.
  - Login/password-reset complete; JWT cookie session persists.
  - Dashboard shows upcoming/past from `/customers/me/bookings`.
  - PDF receipt downloads with ownership enforcement.
- All red placeholders from 04-00 in `ReceiptsControllerTests` + `QuestPdfBookingReceiptGeneratorTests` are green.
</success_criteria>

<output>
After completion, create `.planning/phases/04-b2c-portal-customer-facing/04-01-SUMMARY.md` noting: the `/customers/me/bookings` vs `/customers/{id}/bookings` decision, fare breakdown migration SQL, resend-verification route design, and any Keycloak issues encountered.
</output>
