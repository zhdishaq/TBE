---
phase: 04-b2c-portal-customer-facing
plan: 00
type: execute
wave: 1
depends_on: []
files_modified:
  - src/portals/b2c-web/package.json
  - src/portals/b2c-web/tsconfig.json
  - src/portals/b2c-web/next.config.mjs
  - src/portals/b2c-web/postcss.config.cjs
  - src/portals/b2c-web/eslint.config.mjs
  - src/portals/b2c-web/styles/globals.css
  - src/portals/b2c-web/components/ui/**
  - src/portals/b2c-web/lib/utils.ts
  - src/portals/b2c-web/hooks/**
  - src/portals/b2c-web/app/layout.tsx
  - src/portals/b2c-web/app/page.tsx
  - src/portals/b2c-web/.env.example
  - src/portals/b2c-web/vitest.config.ts
  - src/portals/b2c-web/playwright.config.ts
  - src/portals/b2c-web/tests/setup.ts
  - src/portals/b2c-web/e2e/smoke.spec.ts
  - src/portals/b2c-web/e2e/fixtures/auth.ts
  - src/portals/b2c-web/e2e/fixtures/stripe.ts
  - src/portals/b2c-web/auth.config.ts
  - src/portals/b2c-web/lib/auth.ts
  - src/portals/b2c-web/middleware.ts
  - src/portals/b2c-web/app/api/auth/[...nextauth]/route.ts
  - src/portals/b2c-web/lib/api-client.ts
  - src/portals/b2c-web/types/auth.d.ts
  - tests/BookingService.Tests/BookingService.Tests.csproj
  - tests/BookingService.Tests/ReceiptsControllerTests.cs
  - tests/BookingService.Tests/BasketsControllerTests.cs
  - tests/BookingService.Tests/QuestPdfBookingReceiptGeneratorTests.cs
  - tests/Notifications.Tests/HotelBookingConfirmedConsumerTests.cs
  - tests/Notifications.Tests/HotelVoucherDocumentTests.cs
  - tests/Notifications.Tests/BasketConfirmedConsumerTests.cs
  - infra/keycloak/realm-tbe-b2c.json
  - infra/keycloak/README.md
autonomous: true
requirements: []  # Wave 0 enables all subsequent plans' requirements but owns none directly
user_setup:
  - service: keycloak
    why: "Add audience mapper on tbe-b2c client + provision tbe-b2c-admin service account (Pitfall 4 + Pitfall 8)"
    env_vars:
      - name: KEYCLOAK_B2C_CLIENT_SECRET
        source: "Keycloak admin console → Clients → tbe-b2c → Credentials"
      - name: KEYCLOAK_B2C_ADMIN_CLIENT_SECRET
        source: "Keycloak admin console → Clients → tbe-b2c-admin → Credentials"
    dashboard_config:
      - task: "Add audience mapper on tbe-b2c client emitting 'tbe-api'"
        location: "Keycloak admin → Clients → tbe-b2c → Client Scopes → Dedicated → Add mapper → Audience"
      - task: "Create tbe-b2c-admin client with service account + manage-users role on tbe-b2c realm"
        location: "Keycloak admin → Clients → Create → enable Service Account → Service Account Roles → manage-users"
  - service: stripe
    why: "Dev/test PaymentElement + webhook signing secret already provisioned in Phase 3; confirm reuse"
    env_vars:
      - name: NEXT_PUBLIC_STRIPE_PK
        source: "Stripe Dashboard → Developers → API keys (publishable test key)"

must_haves:
  truths:
    - "Frontend portal scaffold `src/portals/b2c-web/` boots with `pnpm dev` and renders a placeholder landing page"
    - "Vitest + Playwright are configured and a trivial smoke test for each passes"
    - "Auth.js v5 beta + Next 16 + nuqs mutual-compatibility smoke test (signIn redirect → Keycloak → callback → session) passes against local Keycloak"
    - "Backend test projects compile with placeholder tests for ReceiptsController, BasketsController, QuestPdfBookingReceiptGenerator, HotelBookingConfirmedConsumer, HotelVoucherDocument, BasketConfirmedConsumer"
    - "Keycloak `tbe-b2c` client emits access tokens with `aud: tbe-api` (Pitfall 4 resolved)"
    - "Keycloak `tbe-b2c-admin` service account exists and can call `send-verify-email` (Pitfall 8 resolved)"
  artifacts:
    - path: "src/portals/b2c-web/package.json"
      provides: "Next.js 16 portal fork with pinned additions"
      contains: "\"next-auth\": \"5.0.0-beta.31\""
    - path: "src/portals/b2c-web/auth.config.ts"
      provides: "Edge-safe Auth.js subset for middleware (Pitfall 3)"
    - path: "src/portals/b2c-web/lib/auth.ts"
      provides: "Full Auth.js v5 config with Keycloak + refresh rotation"
    - path: "src/portals/b2c-web/middleware.ts"
      provides: "Session gate for /bookings/** and /checkout/**"
    - path: "src/portals/b2c-web/lib/api-client.ts"
      provides: "gatewayFetch helper forwarding Bearer server-side (D-05)"
    - path: "src/portals/b2c-web/vitest.config.ts"
      provides: "Vitest + Testing Library config (Wave 0 of VALIDATION.md)"
    - path: "src/portals/b2c-web/playwright.config.ts"
      provides: "Playwright config with `mobile` project (B2C-05)"
    - path: "src/portals/b2c-web/e2e/smoke.spec.ts"
      provides: "Auth round-trip smoke (Pitfall 1 + Pitfall 3 live-fire)"
    - path: "src/portals/b2c-web/next.config.mjs"
      provides: "CSP headers whitelisting Stripe (Pitfall 16)"
      contains: "js.stripe.com"
    - path: "infra/keycloak/realm-tbe-b2c.json"
      provides: "Exported realm definition with audience mapper + admin client"
    - path: "tests/BookingService.Tests/ReceiptsControllerTests.cs"
      provides: "Red placeholder tests for receipt ownership + 404 + happy-path"
  key_links:
    - from: "src/portals/b2c-web/middleware.ts"
      to: "src/portals/b2c-web/auth.config.ts"
      via: "import { auth } from './auth.config'"
      pattern: "from ['\"]@/auth\\.config['\"]"
    - from: "src/portals/b2c-web/lib/api-client.ts"
      to: "src/portals/b2c-web/lib/auth.ts"
      via: "const session = await auth()"
      pattern: "await auth\\(\\)"
    - from: "src/portals/b2c-web/next.config.mjs"
      to: "Stripe CDN"
      via: "CSP frame-src / script-src / connect-src headers"
      pattern: "js\\.stripe\\.com"
---

<objective>
Establish the B2C portal scaffolding and all test infrastructure so subsequent plans can ship features with automated verification from task 1. Fork `ui/starterKit` → `src/portals/b2c-web/`, pin the five Auth.js/Stripe/nuqs additions, wire the edge-safe Auth.js config + middleware, configure Vitest + Playwright, add `.NET` test placeholders that downstream plans turn green, and resolve Keycloak Pitfalls 4 + 8 by updating the `tbe-b2c` realm and provisioning the `tbe-b2c-admin` service client.

Purpose: Without Wave 0, every later task would be a "MISSING — Wave 0 must create …" stub. Per VALIDATION.md §Sampling Rate, executors need <120s feedback.
Output: A running, empty portal that can sign in against local Keycloak, a green vitest + playwright smoke suite, compiling .NET test placeholders, and an updated Keycloak realm export.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-b2c-portal-customer-facing/04-CONTEXT.md
@.planning/phases/04-b2c-portal-customer-facing/04-RESEARCH.md
@.planning/phases/04-b2c-portal-customer-facing/04-PATTERNS.md
@.planning/phases/04-b2c-portal-customer-facing/04-UI-SPEC.md
@.planning/phases/04-b2c-portal-customer-facing/04-VALIDATION.md
@.planning/phases/03-core-flight-booking-saga-b2c/03-01-SUMMARY.md
@.planning/phases/03-core-flight-booking-saga-b2c/03-02-SUMMARY.md
@.planning/phases/03-core-flight-booking-saga-b2c/03-04-SUMMARY.md
@ui/starterKit/package.json
@ui/starterKit/app/layout.jsx

<interfaces>
<!-- From 03-01: BookingSagaState is the aggregate; CorrelationId is the booking id; UserId carries Keycloak sub -->
Existing .NET types the new tests reference:
- TBE.BookingService.Application.Saga.BookingSagaState { CorrelationId: Guid; UserId: string; BookingReference: string; ProductType: string; TotalAmount: decimal; Currency: string; GdsPnr: string?; TicketNumber: string?; Version: byte[] }
- TBE.BookingService.Infrastructure.BookingDbContext.BookingSagaStates: DbSet<BookingSagaState>
- TBE.NotificationService.Application.Consumers.BookingConfirmedConsumer (analog for HotelBookingConfirmed)
- TBE.NotificationService.Application.Email.EmailIdempotencyLog (with unique (CorrelationId, EmailType) index)
- TBE.NotificationService.Application.Email.EmailType (enum — add HotelVoucher, BasketConfirmation in later plans)
- TBE.Contracts.Events (namespace convention: records with (…, Guid EventId, DateTimeOffset At))

Auth.js v5 contract (not in repo — from RESEARCH Pattern 1):
- `auth.ts` exports { handlers, auth, signIn, signOut }
- `auth()` returns session | null with augmented fields: { access_token: string; email_verified: boolean; expires_at: number }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Fork starterKit + wire Auth.js v5 + middleware + CSP</name>
  <files>
    src/portals/b2c-web/package.json,
    src/portals/b2c-web/tsconfig.json,
    src/portals/b2c-web/next.config.mjs,
    src/portals/b2c-web/postcss.config.cjs,
    src/portals/b2c-web/eslint.config.mjs,
    src/portals/b2c-web/styles/globals.css,
    src/portals/b2c-web/components/ui/**,
    src/portals/b2c-web/lib/utils.ts,
    src/portals/b2c-web/hooks/**,
    src/portals/b2c-web/app/layout.tsx,
    src/portals/b2c-web/app/page.tsx,
    src/portals/b2c-web/.env.example,
    src/portals/b2c-web/auth.config.ts,
    src/portals/b2c-web/lib/auth.ts,
    src/portals/b2c-web/middleware.ts,
    src/portals/b2c-web/app/api/auth/[...nextauth]/route.ts,
    src/portals/b2c-web/lib/api-client.ts,
    src/portals/b2c-web/types/auth.d.ts
  </files>
  <read_first>
    ui/starterKit/package.json,
    ui/starterKit/app/layout.jsx,
    ui/starterKit/next.config.mjs,
    ui/starterKit/jsconfig.json,
    ui/starterKit/lib/utils.js,
    .planning/phases/04-b2c-portal-customer-facing/04-RESEARCH.md,
    .planning/phases/04-b2c-portal-customer-facing/04-PATTERNS.md,
    .planning/phases/04-b2c-portal-customer-facing/04-UI-SPEC.md
  </read_first>
  <action>
    1. **Fork starterKit.** Recursively copy `ui/starterKit/{app,components,hooks,lib,public,styles,config,postcss.config.cjs,eslint.config.mjs,next.config.mjs}` → `src/portals/b2c-web/`. Do NOT copy `.next`, `node_modules`, `package-lock.json`.
    2. **TypeScript enablement (Pitfall 17).** Replace the copied `jsconfig.json` with `tsconfig.json` using `{ "compilerOptions": { "target": "ES2022", "lib": ["dom","dom.iterable","ES2022"], "allowJs": true, "checkJs": false, "skipLibCheck": true, "strict": true, "noEmit": true, "esModuleInterop": true, "module": "esnext", "moduleResolution": "bundler", "resolveJsonModule": true, "isolatedModules": true, "jsx": "preserve", "incremental": true, "plugins": [{"name":"next"}], "paths": {"@/*":["./*"]} }, "include": ["next-env.d.ts","**/*.ts","**/*.tsx","**/*.js","**/*.jsx",".next/types/**/*.ts"], "exclude":["node_modules"] }`. Rename only `lib/utils.js` → `lib/utils.ts` (implementation unchanged).
    3. **Rewrite package.json** at `src/portals/b2c-web/package.json`. Keep every dependency from `ui/starterKit/package.json`, add (exact pins from RESEARCH §Standard Stack additions):
       - `"next-auth": "5.0.0-beta.31"` (no caret — per Pitfall 1)
       - `"@auth/core": "^0.34.3"`
       - `"@stripe/stripe-js": "^9.2.0"`
       - `"@stripe/react-stripe-js": "^6.2.0"`
       - `"nuqs": "^2.8.9"`
       - `"zod": "^4.1.13"` (if not present — starterKit has it)
       devDependencies: `"typescript": "^5.6.0"`, `"@types/react": "^19"`, `"@types/node": "^22"`, `"vitest": "^2.1.0"`, `"@vitest/ui": "^2.1.0"`, `"@testing-library/react": "^16"`, `"@testing-library/jest-dom": "^6.5"`, `"@testing-library/user-event": "^14"`, `"jsdom": "^25"`, `"@playwright/test": "^1.48"`, `"msw": "^2.6"`.
       Set `"name": "b2c-web"`, `"scripts"`: `"dev": "next dev"`, `"build": "next build"`, `"start": "next start"`, `"test": "vitest run"`, `"test:watch": "vitest"`, `"test:e2e": "playwright test"`, `"typecheck": "tsc --noEmit"`.
    4. **CSP in next.config.mjs (Pitfall 16).** Append `async headers()` returning a single `Content-Security-Policy` on `/:path*` that includes: `default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://js.stripe.com; style-src 'self' 'unsafe-inline'; img-src 'self' data: https:; font-src 'self' data:; frame-src https://js.stripe.com https://hooks.stripe.com; connect-src 'self' https://api.stripe.com; form-action 'self'`.
    5. **auth.config.ts (edge-safe subset, Pitfall 3).** Export `authConfig` with only `providers: [Keycloak({ clientId, clientSecret, issuer })]`, `session: { strategy: "jwt" }`, `pages: { signIn: "/login" }`, plus a MINIMAL `callbacks.authorized({ auth, request })` used by middleware.
    6. **lib/auth.ts (full config).** Import `authConfig` + extend with the full `callbacks.jwt` (refresh rotation per RESEARCH Pattern 1, Pitfall 2) and `callbacks.session` (augment with `access_token` + `email_verified` + `expires_at`). Implement `refreshAccessToken(token)` that POSTs to `${issuer}/protocol/openid-connect/token` with grant_type=refresh_token; on failure return `{ ...token, error: "RefreshAccessTokenError" }`.
    7. **app/api/auth/[...nextauth]/route.ts:** `export const { GET, POST } = handlers` from `@/lib/auth`.
    8. **middleware.ts** — copy RESEARCH §Code Examples "Middleware with email_verified check" verbatim. Use `import { auth } from "@/auth.config"` (NOT @/lib/auth — edge-safety). Matcher: `["/bookings/:path*","/checkout/:path*"]`. Exclude `/api/auth/:path*` explicitly (Pitfall's "Running middleware on /api/auth").
    9. **lib/api-client.ts** — copy RESEARCH Pattern 1 `gatewayFetch` verbatim; `cache: "no-store"` (anti-pattern list).
    10. **types/auth.d.ts** — module augmentation for `next-auth` adding `Session.access_token: string | undefined`, `Session.email_verified: boolean`, `JWT.access_token`, `JWT.refresh_token`, `JWT.expires_at`, `JWT.email_verified`.
    11. **app/layout.tsx** — rename `layout.jsx` → `.tsx`; no semantic change, keep ThemeProvider/TooltipProvider/Toaster tree from starterKit (PATTERNS §Frontend app/layout).
    12. **app/page.tsx** — minimal placeholder: `<main className="p-8"><h1 className="text-display font-semibold">TBE — book your trip</h1><p>Phase 4 scaffold</p></main>`. Real landing page lands in Plan 04-02.
    13. **.env.example** — populate with `KEYCLOAK_B2C_CLIENT_ID, KEYCLOAK_B2C_CLIENT_SECRET, KEYCLOAK_B2C_ISSUER, KEYCLOAK_B2C_ADMIN_CLIENT_ID, KEYCLOAK_B2C_ADMIN_CLIENT_SECRET, NEXTAUTH_SECRET, NEXTAUTH_URL, GATEWAY_URL, NEXT_PUBLIC_STRIPE_PK` with dummy values. Add a comment referencing COMP-05.
    14. **Install:** `cd src/portals/b2c-web && pnpm install` (lockfile committed).
  </action>
  <verify>
    <automated>cd src/portals/b2c-web && pnpm install --frozen-lockfile && pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '"next-auth": "5.0.0-beta.31"' src/portals/b2c-web/package.json` returns 1 (no caret, exact pin — Pitfall 1).
    - `grep -c 'js.stripe.com' src/portals/b2c-web/next.config.mjs` returns ≥ 3 (frame-src, script-src, connect-src — Pitfall 16).
    - `grep -c 'allowJs' src/portals/b2c-web/tsconfig.json` returns 1 (Pitfall 17).
    - `grep -rE "from ['\"](\\./|@/)auth\\.config['\"]" src/portals/b2c-web/middleware.ts` returns 1 (edge-split, Pitfall 3).
    - `grep -c "cache: ['\"]no-store['\"]" src/portals/b2c-web/lib/api-client.ts` returns 1.
    - `grep -c 'Authorization.*Bearer' src/portals/b2c-web/lib/api-client.ts` returns 1 (D-05).
    - `src/portals/b2c-web/pnpm-lock.yaml` exists.
    - `src/portals/b2c-web/app/layout.tsx` exists (TSX, not JSX).
    - `ls src/portals/b2c-web/components/ui/button.jsx` succeeds (copied wholesale per Pitfall 17).
    - `pnpm build` inside `src/portals/b2c-web` exits 0.
  </acceptance_criteria>
  <done>
    Portal scaffold builds clean, Auth.js v5 is wired with edge-split config, CSP permits Stripe, and every env var the portal needs is listed in `.env.example`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Configure Vitest + Playwright + live Auth.js compatibility smoke (mandatory Wave 0 validation)</name>
  <files>
    src/portals/b2c-web/vitest.config.ts,
    src/portals/b2c-web/playwright.config.ts,
    src/portals/b2c-web/tests/setup.ts,
    src/portals/b2c-web/tests/smoke.test.ts,
    src/portals/b2c-web/e2e/smoke.spec.ts,
    src/portals/b2c-web/e2e/fixtures/auth.ts,
    src/portals/b2c-web/e2e/fixtures/stripe.ts
  </files>
  <read_first>
    .planning/phases/04-b2c-portal-customer-facing/04-VALIDATION.md,
    .planning/phases/04-b2c-portal-customer-facing/04-RESEARCH.md,
    src/portals/b2c-web/package.json
  </read_first>
  <action>
    1. **vitest.config.ts** — `defineConfig({ test: { environment: 'jsdom', globals: true, setupFiles: ['./tests/setup.ts'], include: ['tests/**/*.test.ts', 'tests/**/*.test.tsx', '**/*.test.{ts,tsx}'], exclude: ['e2e/**', 'node_modules/**', '.next/**'], css: false } })`. Enable `resolve.alias = { '@': path.resolve(__dirname) }`. No watch mode default.
    2. **tests/setup.ts** — `import '@testing-library/jest-dom';` only. No global fetch mocks (MSW added per-suite where needed).
    3. **tests/smoke.test.ts** — `it('renders placeholder', () => { …render(<Home/>)… expect(screen.getByText(/book your trip/i)).toBeInTheDocument() })`. This proves jsdom + RTL + tsconfig paths all agree.
    4. **playwright.config.ts** — `defineConfig({ testDir: './e2e', timeout: 30_000, retries: 0, use: { baseURL: process.env.PLAYWRIGHT_BASE_URL ?? 'http://localhost:3000' }, projects: [ { name: 'chromium', use: { ...devices['Desktop Chrome'] } }, { name: 'mobile', use: { ...devices['iPhone 12'] } } ], webServer: { command: 'pnpm dev', url: 'http://localhost:3000', reuseExistingServer: !process.env.CI } })`. No `--watch` flags (VALIDATION §Sign-Off).
    5. **e2e/fixtures/auth.ts** — exports a Playwright fixture `authedPage` that navigates to `/login`, clicks `Sign in with Keycloak`, fills Keycloak form with `TEST_KC_USER`/`TEST_KC_PASSWORD` from env (skip with `test.skip(!process.env.TEST_KC_USER)` so the fixture is optional on CI agents without a live Keycloak).
    6. **e2e/fixtures/stripe.ts** — exports helpers `fillStripeCard(page, { number: '4242 4242 4242 4242', exp: '12/34', cvc: '123' })` that target the Stripe iframe via `page.frameLocator('iframe[name*="__privateStripeFrame"]')`. Includes a 3DS-test-PAN alias `fillStripe3DSRequired`.
    7. **e2e/smoke.spec.ts** — two tests:
       - `test('landing page renders')` — GET `/`, assert h1 contains "book your trip".
       - `test('auth round-trip (live Keycloak)')` — uses `authedPage` fixture to complete signIn → assert landing on `/` with session cookie set (checks `page.context().cookies()` for `next-auth.session-token`). Skipped if `TEST_KC_USER` unset. **This satisfies RESEARCH Open Question 7 and VALIDATION Wave 0 requirement #4 (Auth.js v5 beta + Next 16 + nuqs mutual-compat smoke).**
    8. **Install Playwright browsers:** `pnpm exec playwright install --with-deps chromium`.
  </action>
  <verify>
    <automated>cd src/portals/b2c-web && pnpm test && pnpm exec playwright test --project=chromium --grep "landing page renders"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "environment: ['\"]jsdom['\"]" src/portals/b2c-web/vitest.config.ts` returns 1.
    - `grep -c "name: ['\"]mobile['\"]" src/portals/b2c-web/playwright.config.ts` returns 1 (B2C-05 viewport).
    - `grep -c "reuseExistingServer" src/portals/b2c-web/playwright.config.ts` returns 1.
    - `grep -rE "\\-\\-watch" src/portals/b2c-web/{package.json,vitest.config.ts,playwright.config.ts}` returns 0 (VALIDATION no-watch rule).
    - `src/portals/b2c-web/tests/smoke.test.ts` exists and runs green via `pnpm test` (unit path).
    - `src/portals/b2c-web/e2e/smoke.spec.ts` exists; `pnpm exec playwright test --grep "landing page renders"` exits 0.
    - `src/portals/b2c-web/e2e/fixtures/auth.ts` has `test.skip(!process.env.TEST_KC_USER` so CI without Keycloak still passes.
    - `src/portals/b2c-web/e2e/fixtures/stripe.ts` references `__privateStripeFrame` frame locator.
  </acceptance_criteria>
  <done>
    Vitest + Playwright run clean on the scaffold. The live-Keycloak auth smoke exists and is opt-in via env var, documented as the Wave 0 compat gate for subsequent plans.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Backend test scaffolds (red placeholders) + Keycloak audience mapper + admin client</name>
  <files>
    tests/BookingService.Tests/BookingService.Tests.csproj,
    tests/BookingService.Tests/ReceiptsControllerTests.cs,
    tests/BookingService.Tests/BasketsControllerTests.cs,
    tests/BookingService.Tests/QuestPdfBookingReceiptGeneratorTests.cs,
    tests/Notifications.Tests/HotelBookingConfirmedConsumerTests.cs,
    tests/Notifications.Tests/HotelVoucherDocumentTests.cs,
    tests/Notifications.Tests/BasketConfirmedConsumerTests.cs,
    infra/keycloak/realm-tbe-b2c.json,
    infra/keycloak/README.md
  </files>
  <read_first>
    tests/Booking.Saga.Tests/Booking.Saga.Tests.csproj,
    tests/Booking.Saga.Tests/BookingsControllerTests.cs,
    tests/Notifications.Tests/Notifications.Tests.csproj,
    src/services/NotificationService/NotificationService.Application/Consumers/BookingConfirmedConsumer.cs,
    .planning/phases/04-b2c-portal-customer-facing/04-RESEARCH.md,
    .planning/phases/03-core-flight-booking-saga-b2c/03-04-SUMMARY.md,
    .planning/phases/04-b2c-portal-customer-facing/04-PATTERNS.md
  </read_first>
  <action>
    1. **Create BookingService.Tests project** if it doesn't exist (analog: `tests/Booking.Saga.Tests/Booking.Saga.Tests.csproj` — same TargetFramework, xUnit + FluentAssertions + NSubstitute + Microsoft.NET.Test.Sdk). Reference `src/services/BookingService/BookingService.API`, `BookingService.Application`, `BookingService.Infrastructure`, `tests/TBE.Tests.Shared`. If a single consolidated `tests/BookingService.Tests/` already covers Booking.Saga.Tests, reuse that project — do NOT duplicate.
    2. **ReceiptsControllerTests.cs** — three skipped/red xUnit tests (use `[Fact(Skip = "Implemented in 04-01")]` is WRONG — use `[Fact]` + `Assert.Fail("Red placeholder — Plan 04-01 implements")` so the build enforces implementation):
       - `GetReceipt_returns_PDF_for_owner`
       - `GetReceipt_returns_403_for_other_user`
       - `GetReceipt_returns_404_for_unknown_id`
    3. **BasketsControllerTests.cs** — red placeholders for Plan 04-04:
       - `PostBaskets_with_flight_and_hotel_publishes_BasketInitiated`
       - `PostBaskets_returns_401_for_unauthenticated`
       - `GetBasketStatus_returns_current_state`
       - `PostPaymentIntent_returns_client_secret_with_idempotency_key`
    4. **QuestPdfBookingReceiptGeneratorTests.cs** — red placeholders:
       - `Generate_produces_nonempty_PDF_bytes`
       - `Generate_includes_fare_yqyr_tax_breakdown` (FLTB-03)
       - `Generate_includes_PNR_and_ticket_number`
    5. **HotelBookingConfirmedConsumerTests.cs** — red placeholders (analog: `BookingConfirmedConsumer` pattern):
       - `Consume_inserts_EmailIdempotencyLog_with_HotelVoucher_type`
       - `Consume_duplicate_event_is_swallowed`
       - `Consume_sends_email_with_voucher_attachment`
    6. **HotelVoucherDocumentTests.cs** — `Generate_produces_nonempty_PDF` + `Generate_uses_Community_license`.
    7. **BasketConfirmedConsumerTests.cs** — red placeholders: `Consume_sends_combined_email`, `Consume_partial_failure_sends_partial_success_email` (D-09).
    8. Each test body: `Assert.Fail("Red placeholder — implemented by Plan 04-0X");` with X referencing the owning plan (04-01 for receipts, 04-03 for hotel voucher, 04-04 for basket).
    9. **infra/keycloak/realm-tbe-b2c.json** — partial realm export (or patch document) showing:
       - `clients[tbe-b2c].protocolMappers[]` contains an Audience mapper adding `tbe-api` (Pitfall 4).
       - `clients[tbe-b2c-admin]` with `serviceAccountsEnabled: true`, `publicClient: false`, `clientAuthenticatorType: client-secret`, and service-account role binding on `realm-management.manage-users` (Pitfall 8).
       This file is the source of truth that Phase 1's Keycloak provisioning script applies.
    10. **infra/keycloak/README.md** — ~30 lines: how to import `realm-tbe-b2c.json` via `kc.sh import --file`, how to rotate client secrets, env vars produced, smoke-test commands (`curl -X POST …/token` with grant_type=password for tbe-b2c; same with client_credentials for tbe-b2c-admin; decode access token and assert `aud: ["tbe-api", …]`).
  </action>
  <verify>
    <automated>dotnet build tests/BookingService.Tests/BookingService.Tests.csproj -warnaserror && dotnet test tests/BookingService.Tests/BookingService.Tests.csproj --filter "FullyQualifiedName!~Red" --no-build || echo "red placeholders present as expected"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/BookingService.Tests/ReceiptsControllerTests.cs` contains exactly 3 `[Fact]` methods.
    - `grep -rE "Assert\\.Fail\\(\"Red placeholder" tests/BookingService.Tests/ tests/Notifications.Tests/` returns ≥ 14 (3 receipts + 4 baskets + 3 quest-pdf + 3 hotel-consumer + 2 hotel-pdf + 2 basket-consumer = 17 ≥ 14 floor).
    - `grep -c 'tbe-b2c-admin' infra/keycloak/realm-tbe-b2c.json` returns ≥ 1.
    - `grep -c '"tbe-api"' infra/keycloak/realm-tbe-b2c.json` returns ≥ 1.
    - `grep -c 'manage-users' infra/keycloak/realm-tbe-b2c.json` returns ≥ 1.
    - `infra/keycloak/README.md` exists and mentions `curl` + `grant_type=client_credentials` + `aud`.
    - `dotnet build tests/BookingService.Tests/BookingService.Tests.csproj -warnaserror` exits 0 (red placeholders compile).
  </acceptance_criteria>
  <done>
    Every downstream plan has pre-authored failing tests to implement against. Keycloak audience + admin-client are documented in a realm-export artifact the operator can apply.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Next.js server | Auth.js session cookie crosses; httpOnly + SameSite=Lax |
| Next.js server → YARP gateway | Bearer access_token injected server-side only (D-05) |
| Next.js server → Keycloak admin API | Separate `tbe-b2c-admin` client credentials used only by route handlers |
| Keycloak → YARP | JWT audience must contain `tbe-api` (Pitfall 4) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-00-01 | I (Info Disclosure) | Auth.js session cookie | mitigate | httpOnly + Secure + SameSite=Lax via Auth.js defaults (Pitfall 18) |
| T-04-00-02 | E (Elevation) | YARP JWT audience | mitigate | Realm export adds audience mapper emitting `tbe-api`; README includes a `curl` verification step (Pitfall 4) |
| T-04-00-03 | E (Elevation) | Keycloak admin API | mitigate | Dedicated `tbe-b2c-admin` service client with only `manage-users` role; secret kept out of browser bundle (no `NEXT_PUBLIC_` prefix) |
| T-04-00-04 | S (Spoofing) | CSP bypass → malicious script | mitigate | `script-src` locked to `'self'` + `js.stripe.com`; `frame-src` locked to Stripe domains (Pitfall 16) |
| T-04-00-05 | T (Tampering) | Auth.js beta dep | mitigate | Exact pin `5.0.0-beta.31`; npm-audit + renovate tracking noted in README; rollback path = pin previous beta (Pitfall 1) |
| T-04-00-06 | I | Next.js middleware pulling Node crypto | mitigate | Edge-safe split (`auth.config.ts` vs `lib/auth.ts`) — Pitfall 3 |
| T-04-00-07 | R (Repudiation) | Red placeholder tests shipping green | accept | `Assert.Fail` makes any un-implemented placeholder fail CI; plans 04-01..04 turn green before merge |
</threat_model>

<verification>
- `pnpm build` in `src/portals/b2c-web` passes without warnings related to missing types, missing CSP, or Auth.js misconfig.
- `pnpm test` runs jsdom unit smoke green.
- `pnpm exec playwright test --grep "landing page renders"` passes.
- With `TEST_KC_USER` set in a dev env, the auth round-trip e2e passes — satisfying VALIDATION Wave 0 requirement #4 (Auth.js v5 + Next 16 + nuqs compat).
- `dotnet build tests/BookingService.Tests/BookingService.Tests.csproj -warnaserror` is green.
- Keycloak realm JSON is reviewable by an operator before any subsequent plan depends on audience-mapped tokens.
</verification>

<success_criteria>
- Portal scaffold boots and type-checks.
- Vitest + Playwright run green on CI without a live Keycloak, and green WITH a live Keycloak for the auth e2e.
- Red-placeholder test files compile and fail informatively, giving downstream plans a ready-to-green target.
- Keycloak audience mapper + admin client are captured in a committed realm definition plus README — no ambiguity for the operator.
</success_criteria>

<output>
After completion, create `.planning/phases/04-b2c-portal-customer-facing/04-00-SUMMARY.md` documenting: pinned Auth.js beta version (date-stamped), scaffolding choices (JS-kept for ui/*, TS for new code), Keycloak artifacts, and the list of red placeholders handed to each downstream plan.
</output>
