---
phase: 06-backoffice-crm
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/services/BackofficeService/BackofficeService.API/Program.cs
  - src/services/BackofficeService/BackofficeService.Infrastructure/BackofficeDbContext.cs
  - src/services/BackofficeService/BackofficeService.Infrastructure/Migrations/20260601000000_CreateDeadLetterQueue.cs
  - src/services/BackofficeService/BackofficeService.Infrastructure/Migrations/20260601000001_CreateWalletCreditRequests.cs
  - src/services/BackofficeService/BackofficeService.Infrastructure/Migrations/20260601000002_CreateCancellationRequests.cs
  - src/services/BackofficeService/BackofficeService.Application/Consumers/ErrorQueueConsumer.cs
  - src/services/BackofficeService/BackofficeService.Application/Controllers/DlqController.cs
  - src/services/BackofficeService/BackofficeService.Application/Controllers/BookingsController.cs
  - src/services/BackofficeService/BackofficeService.Application/Controllers/StaffBookingActionsController.cs
  - src/services/BackofficeService/BackofficeService.Application/Controllers/WalletCreditRequestsController.cs
  - src/services/BackofficeService/BackofficeService.Application/Entities/DeadLetterQueueRow.cs
  - src/services/BackofficeService/BackofficeService.Application/Entities/WalletCreditRequest.cs
  - src/services/BackofficeService/BackofficeService.Application/Entities/CancellationRequest.cs
  - src/services/BookingService/BookingService.Infrastructure/Migrations/20260601100000_AddBookingEventsTable.cs
  - src/services/BookingService/BookingService.Infrastructure/Migrations/20260601100001_AddAppendOnlyRoleGrants.cs
  - src/services/BookingService/BookingService.Infrastructure/Migrations/20260601100002_AddCancellationColumns.cs
  - src/services/BookingService/BookingService.Infrastructure/BookingEventsDbContext.cs
  - src/services/BookingService/BookingService.Application/Saga/BookingEvent.cs
  - src/services/BookingService/BookingService.Application/BookingEventsWriter.cs
  - src/services/PaymentService/PaymentService.Application/Wallet/WalletCreditApprovedConsumer.cs
  - src/services/PaymentService/PaymentService.Infrastructure/Migrations/20260601200000_AddManualCreditKind.cs
  - src/shared/TBE.Contracts/Events/BackofficeEvents.cs
  - src/gateway/TBE.Gateway/appsettings.json
  - src/gateway/TBE.Gateway/Program.cs
  - infra/keycloak/realms/tbe-backoffice-realm.json
  - src/portals/backoffice-web/package.json
  - src/portals/backoffice-web/next.config.mjs
  - src/portals/backoffice-web/auth.config.ts
  - src/portals/backoffice-web/lib/auth.ts
  - src/portals/backoffice-web/lib/api-client.ts
  - src/portals/backoffice-web/lib/rbac.ts
  - src/portals/backoffice-web/middleware.ts
  - src/portals/backoffice-web/types/auth.d.ts
  - src/portals/backoffice-web/types/ui.d.ts
  - src/portals/backoffice-web/components/layout/BackofficePortalBadge.tsx
  - src/portals/backoffice-web/components/layout/FourEyesApprovalBadge.tsx
  - src/portals/backoffice-web/components/ui/
  - src/portals/backoffice-web/app/(portal)/layout.tsx
  - src/portals/backoffice-web/app/(portal)/bookings/page.tsx
  - src/portals/backoffice-web/app/(portal)/bookings/[id]/page.tsx
  - src/portals/backoffice-web/app/(portal)/bookings/cancellations/page.tsx
  - src/portals/backoffice-web/app/(portal)/operations/dlq/page.tsx
  - src/portals/backoffice-web/app/(portal)/finance/wallet-credits/page.tsx
  - src/portals/backoffice-web/app/api/bookings/route.ts
  - src/portals/backoffice-web/app/api/bookings/[id]/cancel/route.ts
  - src/portals/backoffice-web/app/api/bookings/[id]/cancel/approve/route.ts
  - src/portals/backoffice-web/app/api/wallet-credits/route.ts
  - src/portals/backoffice-web/app/api/wallet-credits/[id]/approve/route.ts
  - src/portals/backoffice-web/app/api/dlq/route.ts
  - src/portals/backoffice-web/app/api/dlq/[id]/requeue/route.ts
  - src/portals/backoffice-web/app/api/dlq/[id]/resolve/route.ts
  - tests/TBE.BackofficeService.Tests/TBE.BackofficeService.Tests.csproj
  - tests/TBE.BackofficeService.Tests/TestFixtures/SqlServerFixture.cs
  - tests/TBE.BackofficeService.Tests/TestFixtures/RabbitMqFixture.cs
  - tests/TBE.BackofficeService.Tests/BookingEventsAppendOnlyTests.cs
  - tests/TBE.BackofficeService.Tests/BookingEventsSnapshotTests.cs
  - tests/TBE.BackofficeService.Tests/StaffCancelModifyTests.cs
  - tests/TBE.BackofficeService.Tests/ManualWalletCreditFourEyesTests.cs
  - tests/TBE.BackofficeService.Tests/DeadLetterQueueTests.cs
  - tests/TBE.BackofficeService.Tests/UnifiedBookingListTests.cs
autonomous: false
requirements: [BO-01, BO-03, BO-04, BO-05, BO-09, BO-10]
user_setup:
  - service: keycloak
    why: "tbe-backoffice realm must be re-imported with 4 ops-* roles (D-46)"
    env_vars:
      - name: KEYCLOAK_BACKOFFICE_ISSUER
        source: "Keycloak Dashboard → Realms → tbe-backoffice → well-known/openid-configuration issuer"
      - name: KEYCLOAK_BACKOFFICE_CLIENT_ID
        source: "Keycloak Dashboard → tbe-backoffice realm → Clients → tbe-backoffice-ui"
      - name: KEYCLOAK_BACKOFFICE_CLIENT_SECRET
        source: "Keycloak Dashboard → tbe-backoffice realm → Clients → tbe-backoffice-ui → Credentials"
    dashboard_config:
      - task: "Import infra/keycloak/realms/tbe-backoffice-realm.json (overwrites placeholder realm with wrong role names)"
        location: "Keycloak admin console → Realms → Add realm → Import"
      - task: "Create test users: ops-admin-1, ops-cs-1, ops-finance-1, ops-read-1"
        location: "Keycloak admin console → tbe-backoffice realm → Users"

must_haves:
  truths:
    - "Backoffice staff can open a unified list of all bookings (B2C + B2B + Manual) with channel chip, status, PNR, customer/agency, total, and can drill into any row."
    - "BookingEvents table rejects UPDATE and DELETE statements at the SQL Server engine (not trigger, not app code) via DENY role grant."
    - "Every booking state transition writes exactly one BookingEvents row with a full Snapshot JSON envelope including pricing breakdown and supplier response."
    - "ops-cs can open a cancellation request with a required reason code; ops-admin (different user) can approve it; self-approval returns 403 with problem+json."
    - "ops-finance can open a manual wallet credit request; ops-admin (different user) approves it; on approval a payment.WalletTransactions row with Kind=ManualCredit is appended atomically."
    - "Failed messages on MassTransit _error queues are persisted into backoffice.DeadLetterQueue with full envelope; ops-admin can requeue or mark resolved with reason."
    - "backoffice-web portal authenticates against tbe-backoffice realm, uses slate-900 accent, blocks non-ops-admin from /approvals, /finance/wallet-credits, /operations/dlq, /bookings/cancellations."
  artifacts:
    - path: "src/services/BookingService/BookingService.Infrastructure/Migrations/20260601100000_AddBookingEventsTable.cs"
      provides: "dbo.BookingEvents table (EventId, BookingId, EventType, OccurredAt, Actor, CorrelationId, Snapshot nvarchar(max))"
      contains: "migrationBuilder.CreateTable"
    - path: "src/services/BookingService/BookingService.Infrastructure/Migrations/20260601100001_AddAppendOnlyRoleGrants.cs"
      provides: "SQL Server role booking_events_writer with GRANT INSERT,SELECT + DENY UPDATE,DELETE"
      contains: "DENY UPDATE, DELETE ON dbo.BookingEvents"
    - path: "src/services/BookingService/BookingService.Infrastructure/BookingEventsDbContext.cs"
      provides: "Writer-only EF DbContext with its own connection string (bypasses ChangeTracker UPDATE attempts)"
      contains: "DbSet<BookingEvent>"
    - path: "src/services/BackofficeService/BackofficeService.Application/Consumers/ErrorQueueConsumer.cs"
      provides: "Raw-JSON consumer on _error queues that writes DeadLetterQueueRow"
      contains: "IConsumer<JsonObject>"
    - path: "src/services/BackofficeService/BackofficeService.Application/Controllers/WalletCreditRequestsController.cs"
      provides: "POST /api/backoffice/wallet-credits + POST /{id}/approve with self-approval guard"
      contains: "four-eyes-self-approval"
    - path: "src/services/BackofficeService/BackofficeService.Application/Controllers/StaffBookingActionsController.cs"
      provides: "POST /api/backoffice/bookings/{id}/cancel + POST /{id}/cancel/approve"
      contains: "PendingApproval"
    - path: "src/services/BackofficeService/BackofficeService.Application/Controllers/BookingsController.cs"
      provides: "GET /api/backoffice/bookings unified list"
      contains: "RequireRole"
    - path: "src/services/BackofficeService/BackofficeService.Application/Controllers/DlqController.cs"
      provides: "GET /api/backoffice/dlq + POST /{id}/requeue + POST /{id}/resolve"
      contains: "RequeueCount"
    - path: "src/portals/backoffice-web/app/(portal)/bookings/page.tsx"
      provides: "Unified booking list RSC"
      min_lines: 40
    - path: "src/portals/backoffice-web/app/(portal)/operations/dlq/page.tsx"
      provides: "DLQ list with 60s poll"
      min_lines: 40
    - path: "src/portals/backoffice-web/app/(portal)/finance/wallet-credits/page.tsx"
      provides: "Wallet credit request queue + approval dialog"
      min_lines: 40
    - path: "infra/keycloak/realms/tbe-backoffice-realm.json"
      provides: "Keycloak realm with 4 ops-* roles, tbe-backoffice-ui OIDC client, tbe-backoffice-admin service client, tbe-api audience mapper"
      contains: "ops-admin"
    - path: "tests/TBE.BackofficeService.Tests/BookingEventsAppendOnlyTests.cs"
      provides: "Integration test proving UPDATE + DELETE on dbo.BookingEvents throws SqlException"
      contains: "Assert.Throws<SqlException>"
  key_links:
    - from: "src/services/BookingService/BookingService.Application/Saga/BookingSaga.cs"
      to: "src/services/BookingService/BookingService.Application/BookingEventsWriter.cs"
      via: "WriteAsync invoked on every DuringAny transition"
      pattern: "_bookingEventsWriter\\.WriteAsync"
    - from: "src/services/BackofficeService/BackofficeService.Application/Consumers/ErrorQueueConsumer.cs"
      to: "src/services/BackofficeService/BackofficeService.Infrastructure/BackofficeDbContext.cs"
      via: "_db.DeadLetterQueue.Add + SaveChangesAsync inside outbox transaction"
      pattern: "DeadLetterQueue\\.Add"
    - from: "src/services/BackofficeService/BackofficeService.Application/Controllers/WalletCreditRequestsController.cs"
      to: "src/services/PaymentService/PaymentService.Application/Wallet/WalletCreditApprovedConsumer.cs"
      via: "IPublishEndpoint.Publish(WalletCreditApproved) inside EF outbox"
      pattern: "Publish.*WalletCreditApproved"
    - from: "src/portals/backoffice-web/lib/auth.ts"
      to: "infra/keycloak/realms/tbe-backoffice-realm.json"
      via: "OIDC discovery against KEYCLOAK_BACKOFFICE_ISSUER"
      pattern: "tbe-backoffice"
    - from: "src/gateway/TBE.Gateway/appsettings.json"
      to: "src/services/BackofficeService/BackofficeService.API/Program.cs"
      via: "ReverseProxy route /api/backoffice/* → BackofficeService, scheme Backoffice"
      pattern: "api/backoffice"
---

<objective>
Ship the Phase 6 foundations: stand up BackofficeService + backoffice-web portal, enforce BO-04 DB-level append-only BookingEvents, deliver BO-01 unified list, BO-03 staff cancel/modify with 4-eyes, BO-05 full snapshot storage, D-39 manual wallet credit with 4-eyes, and BO-09/10 dead-letter queue visibility and manual recovery. Plans 06-02/03/04 depend on this wave.

Purpose: every other Phase 6 plan consumes at least one artifact from this wave — the BackofficeService auth + policies + DbContext + consumer registration template (Plans 06-02/03 reuse it), the BookingEvents table and writer (Plan 06-02 manual bookings write a ManualBookingCreated event; Plan 06-04 CRM projections consume the same events), the DLQ table and consumer (Plan 06-02 recon job surfaces errors via the same DLQ), the 4-eyes approval pattern (Plan 06-03 commission payout uses a single-operator approve action per D-54; 4-eyes — this plan's workflow — is limited to D-48 scope: booking cancellation and manual wallet credit only), and the backoffice-web portal scaffold (all subsequent plans add pages beneath it).

Output:
- BackofficeService.API Program.cs wired with JWT (Backoffice scheme), 4 policies (ops-read/cs/finance/admin), MassTransit outbox, controllers, ErrorQueueConsumer registrations
- BackofficeDbContext with DeadLetterQueue + WalletCreditRequests + CancellationRequests tables + outbox
- BookingEvents table and role-grant enforcement (separate DbContext and SQL login)
- DlqController + BookingsController (unified list) + StaffBookingActionsController + WalletCreditRequestsController
- backoffice-web portal (forked from b2b-web with slate-900 accent, no Stripe CSP, 4 role gates)
- tbe-backoffice Keycloak realm rewrite with 4 ops-* roles
- YARP gateway route /api/backoffice/* added, Backoffice scheme wired
- Wave 0 test project with Testcontainers SqlServer + MassTransit harness + 6 integration test classes
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

@.planning/phases/06-backoffice-crm/06-CONTEXT.md
@.planning/phases/06-backoffice-crm/06-RESEARCH.md
@.planning/phases/06-backoffice-crm/06-PATTERNS.md
@.planning/phases/06-backoffice-crm/06-UI-SPEC.md
@.planning/phases/06-backoffice-crm/06-VALIDATION.md

@.planning/phases/05-b2b-agent-portal/05-04-SUMMARY.md
@.planning/phases/03-core-flight-booking-saga-b2c/03-01-SUMMARY.md

<!-- Source files the executor must read — these are the canonical analogs -->
@src/services/PaymentService/PaymentService.API/Program.cs
@src/services/PaymentService/PaymentService.API/Controllers/WalletController.cs
@src/services/BookingService/BookingService.API/Controllers/InvoicesController.cs
@src/services/BookingService/BookingService.API/Controllers/AgentBookingsController.cs
@src/services/BookingService/BookingService.Application/Saga/BookingSaga.cs
@src/services/BookingService/BookingService.Infrastructure/BookingDbContext.cs
@src/services/BookingService/BookingService.Application/Consumers/CompensationConsumers/SagaDeadLetterSink.cs
@src/services/BookingService/BookingService.Infrastructure/SagaDeadLetterStore.cs
@src/services/BookingService/BookingService.Infrastructure/Migrations/20260520000000_AddB2BBookingColumns.cs
@src/services/PaymentService/PaymentService.Infrastructure/Migrations/20260417000000_AddWalletAndStripe.cs
@src/services/PaymentService/PaymentService.Infrastructure/Migrations/20260525000000_AddAgencyWallet.cs
@src/services/BackofficeService/BackofficeService.API/Program.cs
@src/services/BackofficeService/BackofficeService.Infrastructure/BackofficeDbContext.cs
@src/gateway/TBE.Gateway/Program.cs
@src/gateway/TBE.Gateway/appsettings.json
@infra/keycloak/realm-tbe-b2b.json
@infra/keycloak/realms/tbe-backoffice-realm.json
@src/portals/b2b-web/auth.config.ts
@src/portals/b2b-web/lib/auth.ts
@src/portals/b2b-web/lib/api-client.ts
@src/portals/b2b-web/middleware.ts
@src/portals/b2b-web/next.config.mjs
@src/portals/b2b-web/app/(portal)/layout.tsx
@src/portals/b2b-web/app/(portal)/bookings/page.tsx
@src/portals/b2b-web/components/layout/agent-portal-badge.tsx
@tests/TBE.BookingService.Tests/TBE.BookingService.Tests.csproj

<interfaces>
<!-- Key types the executor needs. Use these directly. -->

From src/shared/TBE.Contracts/Events/SagaEvents.cs:
```csharp
public record BookingConfirmed(Guid BookingId, Guid? CustomerId, Guid? AgencyId, string Channel, decimal GrossAmount, decimal NetAmount, decimal MarkupAmount, decimal CommissionAmount, string Currency, DateTime At);
public record BookingCancelled(Guid BookingId, Guid? CustomerId, Guid? AgencyId, string Reason, DateTime At);
public record TicketIssued(Guid BookingId, string Pnr, string TicketNumber, DateTime At);
```

New contracts THIS plan defines in src/shared/TBE.Contracts/Events/BackofficeEvents.cs:
```csharp
public record WalletCreditRequested(Guid RequestId, Guid AgencyId, decimal Amount, string Currency, string ReasonCode, Guid? LinkedBookingId, string Notes, string RequestedBy, DateTime RequestedAt, DateTime ExpiresAt);
public record WalletCreditApproved(Guid RequestId, Guid AgencyId, decimal Amount, string Currency, string ReasonCode, Guid? LinkedBookingId, string RequestedBy, string ApprovedBy, string ApprovalNotes, DateTime ApprovedAt);
public record BookingCancelledByStaff(Guid BookingId, string ReasonCode, string Reason, string RequestedBy, string ApprovedBy, DateTime ApprovedAt);
```

Reason code enums (persisted as nvarchar(64) with CHECK constraint):
- Wallet credit reasons: RefundedBooking | GoodwillCredit | DisputeResolution | SupplierRefundPassthrough (D-53 locked)
- Cancellation reasons: CustomerRequest | SupplierInitiated | FareRuleViolation | FraudSuspected | DuplicateBooking | Other

4-eyes state machine: PendingApproval → Approved | Denied | Expired (72h TTL per D-48 + RESEARCH anti-pattern 5)

DeadLetterQueue schema (D-58):
- Id GUID PK, MessageId GUID, CorrelationId GUID NULL, MessageType nvarchar(256), OriginalQueue nvarchar(256), Payload nvarchar(max), FailureReason nvarchar(1000), FirstFailedAt datetime2, LastRequeuedAt datetime2 NULL, RequeueCount int DEFAULT 0, ResolvedAt datetime2 NULL, ResolvedBy nvarchar(128) NULL, ResolutionReason nvarchar(500) NULL
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Wave 0 Playwright bootstrap — fork b2b-web Playwright config for backoffice-web</name>
  <files>
    src/portals/backoffice-web/tests/e2e/playwright.config.ts,
    src/portals/backoffice-web/tests/e2e/.gitkeep
  </files>
  <action>
VALIDATION.md line 73 names `src/portals/backoffice-web/tests/e2e/` as a Wave 0 deliverable. Pin it here as a discrete task so Pitfall 19 (cookie isolation across portals) is enforced in the e2e harness from day 1.

**A.** Copy `src/portals/b2b-web/playwright.config.ts` → `src/portals/backoffice-web/tests/e2e/playwright.config.ts` (note the path change: b2b-web stores it at the portal root, backoffice-web stores it under `tests/e2e/` per VALIDATION.md §Wave 0). If the path layout in b2b-web evolves first, follow b2b-web's structure byte-for-byte and mirror it into backoffice-web.

**B.** Apply these deltas to the forked config (keep everything else identical — no novel test machinery):
1. `use.baseURL = 'http://localhost:3003'` (backoffice-web dev port — Pattern M already documents 3002 for portal local; test harness uses 3003 to avoid colliding with a running dev server; adjust if dev port differs in Plan 06-01 Task 2 `package.json` — keep the two in sync).
2. `projects` array: single `{ name: 'backoffice', testMatch: '**/*.spec.ts' }` entry — rename any `b2b` project entry from the source.
3. `use.storageState` keyed off an env var named `BACKOFFICE_STORAGE_STATE` (not `B2B_STORAGE_STATE`) so the authenticated-user fixture file for this suite is isolated from the b2b-web harness.
4. **Cookie isolation (Pitfall 19 pin):** add an explicit `use.extraHTTPHeaders = {}` comment block referencing the `__Host-backoffice.session` cookie prefix introduced in Plan 06-01 Task 2 (`auth.config.ts` will use `__Secure-tbe-backoffice.session-token`; E2E harness re-uses a storageState written with that exact cookie name). Add an inline comment: `// Pitfall 19: backoffice cookie MUST be __Host-backoffice.session / __Secure-tbe-backoffice.session-token — NEVER reuse b2b-web's __Secure-tbe-b2b.session-token cookie.`
5. If the source config sets any `webServer` block pointing at `pnpm dev` for b2b-web, update it to `pnpm --filter ./src/portals/backoffice-web run dev` and the expected URL to `http://localhost:3003`.

**C.** Create a `.gitkeep` in `src/portals/backoffice-web/tests/e2e/` so the directory survives in git even before `*.spec.ts` files land (Plans 06-02/03/04 will add spec files as their UI pages ship).

**D.** Wire into `package.json` (Plan 06-01 Task 2 writes the portal `package.json`; this task runs BEFORE that task by being T1, so either: (a) write the playwright config now and rely on Task 2 to add the `test:e2e` script when it writes `package.json` — safer; or (b) document the expected script in this task's `<done>` and let Task 2 include it). **Chosen:** option (a) — this task writes the config file only; Task 2 (bootstrap) adds the npm script `"test:e2e": "playwright test --config=tests/e2e/playwright.config.ts"` to `package.json` when it forks it.

This task is intentionally tiny (~15-25 LOC across 2 files) and is sequenced FIRST to pin the cookie-isolation contract before the Keycloak realm / auth.config.ts fork happens in Task 2. The harness won't actually execute anything green until Plans 06-02/03/04 write spec files — this is the scaffold.
  </action>
  <verify>
    <automated>test -f src/portals/backoffice-web/tests/e2e/playwright.config.ts &amp;&amp; grep -q "__Host-backoffice" src/portals/backoffice-web/tests/e2e/playwright.config.ts &amp;&amp; grep -q "localhost:3003" src/portals/backoffice-web/tests/e2e/playwright.config.ts</automated>
  </verify>
  <done>
`src/portals/backoffice-web/tests/e2e/playwright.config.ts` exists, forked from b2b-web, with baseURL 3003, project name 'backoffice', and Pitfall 19 cookie-isolation comment. `.gitkeep` ensures the e2e directory is tracked. The subsequent Task 2 (bootstrap) adds the matching `test:e2e` npm script.
  </done>
</task>

<task type="auto">
  <name>Task 2: Wave 0 — BackofficeService test project, fixtures, and red placeholders</name>
  <files>
    tests/TBE.BackofficeService.Tests/TBE.BackofficeService.Tests.csproj,
    tests/TBE.BackofficeService.Tests/TestFixtures/SqlServerFixture.cs,
    tests/TBE.BackofficeService.Tests/TestFixtures/RabbitMqFixture.cs,
    tests/TBE.BackofficeService.Tests/BookingEventsAppendOnlyTests.cs,
    tests/TBE.BackofficeService.Tests/BookingEventsSnapshotTests.cs,
    tests/TBE.BackofficeService.Tests/StaffCancelModifyTests.cs,
    tests/TBE.BackofficeService.Tests/ManualWalletCreditFourEyesTests.cs,
    tests/TBE.BackofficeService.Tests/DeadLetterQueueTests.cs,
    tests/TBE.BackofficeService.Tests/UnifiedBookingListTests.cs
  </files>
  <action>
Create a new xUnit test project (net8.0) mirroring `tests/TBE.BookingService.Tests/TBE.BookingService.Tests.csproj`. Reference packages: xunit 2.9.0, Microsoft.NET.Test.Sdk 17.11.1, Testcontainers.MsSql 3.10.0, Testcontainers.RabbitMq 3.10.0, MassTransit.TestFramework 9.1.0, Microsoft.AspNetCore.Mvc.Testing 8.0.11 (net8-pinned per Plan 05-03 deviation), NSubstitute 5.1.0, FluentAssertions 6.12.2. Reference the three BackofficeService projects.

Add `tests/TBE.BackofficeService.Tests/TestFixtures/SqlServerFixture.cs` mirroring the pattern from `tests/TBE.BookingService.Tests/` (locate the existing SqlServerFixture — if named `MsSqlFixture` or similar, copy its shape; the expectation: Testcontainers MsSql container, spins up per `IAsyncLifetime`, exposes a connection string, runs EF migrations on initialisation). Add `RabbitMqFixture.cs` that exposes a `Testcontainers.RabbitMq` container; tests can point `AddTbeMassTransitWithRabbitMq` at it via the container's AMQP URL.

Create 6 red-placeholder test classes, one per VALIDATION.md Task 6-01-01..06, each tagged `[Trait("Category", "Phase06")]` and `[Trait("Category", "RedPlaceholder")]`. Each has exactly ONE `[Fact]` method matching the filter name from VALIDATION.md:

1. `BookingEventsAppendOnlyTests.cs` — `UpdateAndDelete_are_rejected_at_engine_level_D49()` — Assert.Fail("MISSING — Plan 06-01 Task 5 implements DENY role grant migration")
2. `BookingEventsSnapshotTests.cs` — `Every_event_persists_full_snapshot_json_BO05()` — Assert.Fail("MISSING — Plan 06-01 Task 5 wires BookingEventsWriter into saga")
3. `StaffCancelModifyTests.cs` — `OpsCs_can_open_request_OpsAdmin_approves_logged_in_BookingEvents_BO03()` — Assert.Fail("MISSING — Plan 06-01 Task 6")
4. `ManualWalletCreditFourEyesTests.cs` — `Self_approval_returns_403_different_admin_approves_atomic_write_D39()` — Assert.Fail("MISSING — Plan 06-01 Task 6")
5. `DeadLetterQueueTests.cs` — `Error_queue_consumer_persists_envelope_requeue_preserves_correlation_BO09_BO10()` — Assert.Fail("MISSING — Plan 06-01 Task 4")
6. `UnifiedBookingListTests.cs` — `Returns_B2C_B2B_Manual_channels_with_role_based_filter_BO01()` — Assert.Fail("MISSING — Plan 06-01 Task 7")

Run `dotnet test tests/TBE.BackofficeService.Tests/TBE.BackofficeService.Tests.csproj --filter "Category=RedPlaceholder" --no-restore` — must compile and all 6 tests must fail with the `MISSING —` message (proving Wave 0 wires the fixtures without false greens). Run `dotnet test tests/TBE.BackofficeService.Tests/TBE.BackofficeService.Tests.csproj --filter "Category!=RedPlaceholder" --no-restore` — must pass with zero tests executed (proves CI baseline stays green).
  </action>
  <verify>
    <automated>dotnet test tests/TBE.BackofficeService.Tests/TBE.BackofficeService.Tests.csproj --filter "FullyQualifiedName~Phase06&amp;Category=RedPlaceholder" --no-restore</automated>
  </verify>
  <done>
csproj compiles, 2 fixtures exist, 6 red-placeholder classes each with exactly 1 `[Fact]` that fails with a `MISSING —` stub (6 failures, 0 passes when Category=RedPlaceholder; 0 tests when Category!=RedPlaceholder).
  </done>
</task>

<task type="auto">
  <name>Task 3: BackofficeService bootstrap — Program.cs + DbContext + Keycloak realm + YARP route + portal fork</name>
  <files>
    src/services/BackofficeService/BackofficeService.API/Program.cs,
    src/services/BackofficeService/BackofficeService.Infrastructure/BackofficeDbContext.cs,
    src/services/BackofficeService/BackofficeService.Infrastructure/Migrations/20260601000000_CreateDeadLetterQueue.cs,
    src/services/BackofficeService/BackofficeService.Infrastructure/Migrations/20260601000001_CreateWalletCreditRequests.cs,
    src/services/BackofficeService/BackofficeService.Infrastructure/Migrations/20260601000002_CreateCancellationRequests.cs,
    src/services/BackofficeService/BackofficeService.Application/Entities/DeadLetterQueueRow.cs,
    src/services/BackofficeService/BackofficeService.Application/Entities/WalletCreditRequest.cs,
    src/services/BackofficeService/BackofficeService.Application/Entities/CancellationRequest.cs,
    src/gateway/TBE.Gateway/Program.cs,
    src/gateway/TBE.Gateway/appsettings.json,
    infra/keycloak/realms/tbe-backoffice-realm.json,
    src/portals/backoffice-web/package.json,
    src/portals/backoffice-web/next.config.mjs,
    src/portals/backoffice-web/auth.config.ts,
    src/portals/backoffice-web/lib/auth.ts,
    src/portals/backoffice-web/lib/api-client.ts,
    src/portals/backoffice-web/lib/rbac.ts,
    src/portals/backoffice-web/middleware.ts,
    src/portals/backoffice-web/types/auth.d.ts,
    src/portals/backoffice-web/types/ui.d.ts,
    src/portals/backoffice-web/components/layout/BackofficePortalBadge.tsx,
    src/portals/backoffice-web/components/layout/FourEyesApprovalBadge.tsx,
    src/portals/backoffice-web/components/ui/,
    src/portals/backoffice-web/app/(portal)/layout.tsx
  </files>
  <action>
**A. BackofficeService.API/Program.cs (replace skeleton)** — mirror `src/services/PaymentService/PaymentService.API/Program.cs` structure. The existing skeleton has Serilog + DbContext + MassTransit outbox + health checks; ADD:

1. JWT Bearer authentication (scheme `"Backoffice"`) — issuer from `Keycloak:Backoffice:Authority`, audience `tbe-api`, `ValidateAudience=true`. Mirror `OnTokenValidated` handler from PaymentService Plan 05-01 that projects `realm_access.roles` into flat `roles` claims.
2. 4 named authorization policies, each calling `.AddAuthenticationSchemes("Backoffice")` (Pitfall 4 pin):
   - `BackofficeReadPolicy` — `RequireRole("ops-read", "ops-cs", "ops-finance", "ops-admin")`
   - `BackofficeCsPolicy` — `RequireRole("ops-cs", "ops-admin")`
   - `BackofficeFinancePolicy` — `RequireRole("ops-finance", "ops-admin")`
   - `BackofficeAdminPolicy` — `RequireRole("ops-admin")`
3. `builder.Services.AddControllers()` and `app.MapControllers()`.
4. MassTransit consumer registration: `x.AddConsumer<ErrorQueueConsumer>()` (consumer code lands in Task 3, stub it as placeholder `public class ErrorQueueConsumer : IConsumer<JsonObject> { public Task Consume(ConsumeContext<JsonObject> ctx) => Task.CompletedTask; }` so Program.cs compiles; Task 3 replaces the body).
5. Receive endpoint registrations for the known `_error` queues (enumerate from existing services' consumers). Static list for v1 (Program.cs comment cites RESEARCH Pattern 2 §"which _error queues to bind"): `booking-saga_error`, `stripe-webhook_error`, `wallet-low-balance_error`, `wallet-top-up_error`, `ticketing-deadline_error`, `booking-confirmed_error`, `booking-cancelled_error`, `ticket-issued_error`, `user-registered_error`, `wallet-top-up-confirmed_error`. Each registered with `cfg.ReceiveEndpoint(name, e => { e.UseRawJsonDeserializer(); e.ConfigureConsumer<ErrorQueueConsumer>(context); })`.
6. `app.UseAuthentication(); app.UseAuthorization();` between `UseRouting` and `MapControllers`.

**B. BackofficeDbContext.cs (replace skeleton)** — extend `DbContext` with `DbSet<DeadLetterQueueRow> DeadLetterQueue`, `DbSet<WalletCreditRequest> WalletCreditRequests`, `DbSet<CancellationRequest> CancellationRequests`. In `OnModelCreating`, call `modelBuilder.AddInboxStateEntity(); AddOutboxMessageEntity(); AddOutboxStateEntity();` (MassTransit outbox per BookingDbContext pattern). Map tables to schema `"backoffice"`.

**C. Three EF migrations** (timestamps 20260601000000, 20260601000001, 20260601000002):
- `CreateDeadLetterQueue` — schema `backoffice`, table `DeadLetterQueue` exactly matching the D-58 schema in `<interfaces>`. Non-unique filtered index on `MessageId WHERE ResolvedAt IS NULL`. Index on `(FirstFailedAt DESC)`.
- `CreateWalletCreditRequests` — schema `backoffice`, table `WalletCreditRequests`: Id GUID PK, AgencyId GUID, Amount decimal(18,4) CHECK (Amount > 0 AND Amount <= 100000), Currency char(3), ReasonCode nvarchar(64) CHECK (ReasonCode IN ('RefundedBooking','GoodwillCredit','DisputeResolution','SupplierRefundPassthrough')), LinkedBookingId GUID NULL, Notes nvarchar(1000), RequestedBy nvarchar(128), RequestedAt datetime2 DEFAULT SYSUTCDATETIME(), ExpiresAt datetime2, Status nvarchar(32) DEFAULT 'PendingApproval' CHECK (Status IN ('PendingApproval','Approved','Denied','Expired')), ApprovedBy nvarchar(128) NULL, ApprovedAt datetime2 NULL, ApprovalNotes nvarchar(500) NULL. Non-unique index on `(Status, RequestedAt DESC)`.
- `CreateCancellationRequests` — schema `backoffice`, table `CancellationRequests`: Id GUID PK, BookingId GUID, ReasonCode nvarchar(64) CHECK (...see <interfaces>...), Reason nvarchar(500), RequestedBy nvarchar(128), RequestedAt datetime2 DEFAULT SYSUTCDATETIME(), ExpiresAt datetime2, Status nvarchar(32) DEFAULT 'PendingApproval' CHECK (...), ApprovedBy nvarchar(128) NULL, ApprovedAt datetime2 NULL, ApprovalReason nvarchar(500) NULL. Non-unique index on `(BookingId, Status)` and `(Status, RequestedAt DESC)`.

**D. YARP Gateway** — `src/gateway/TBE.Gateway/Program.cs`: add `.AddJwtBearer("Backoffice", options => { Authority = "{keycloakBaseUrl}/realms/tbe-backoffice"; Audience = "tbe-api"; ValidateAudience = true; ValidateIssuer = true; })`. Register policy `"BackofficePolicy"` that `.AddAuthenticationSchemes("Backoffice").RequireAuthenticatedUser()`. `src/gateway/TBE.Gateway/appsettings.json` — add ReverseProxy route `{ "Match": { "Path": "/api/backoffice/{**catch-all}" }, "AuthorizationPolicy": "BackofficePolicy", "ClusterId": "backoffice-cluster" }` + cluster pointing to `http://backoffice-api:8080`.

**E. Keycloak realm (`infra/keycloak/realms/tbe-backoffice-realm.json`) — REWRITE** (current file has wrong role names per RESEARCH Runtime State Inventory):
- `realm: "tbe-backoffice"`, enabled true, accessTokenLifespan 1800.
- `roles.realm`: `[{ name: "ops-admin", ... }, { name: "ops-cs", ... }, { name: "ops-finance", ... }, { name: "ops-read", ... }]` — with descriptions.
- `clients[0]` = `tbe-backoffice-ui` — public OIDC client, standardFlowEnabled true, redirectUris `["http://localhost:3002/backoffice/api/auth/callback/*", "http://localhost:3002/backoffice/*"]`, webOrigins `["http://localhost:3002"]`, protocolMappers: audience mapper mapping to `tbe-api`, realm-access-to-roles mapper (standard).
- `clients[1]` = `tbe-backoffice-admin` — confidential client-credentials client with service-accounts-enabled, for future Keycloak Admin API access.
- `users[]` seed: `ops-admin-1`, `ops-cs-1`, `ops-finance-1`, `ops-read-1` each with realmRoles including their role + `default-roles-tbe-backoffice`. Credentials: `[{ type: "password", value: "test1234", temporary: false }]`.

**F. backoffice-web portal fork** — copy `src/portals/b2b-web/**` → `src/portals/backoffice-web/**` (fork, not symlink). Apply deltas from PATTERNS.md Pattern M:
- `package.json` — bump `name` to `tbe-backoffice-web`, port 3002 (dev: `"dev": "next dev -p 3002"`).
- `next.config.mjs` — `basePath: '/backoffice'`. **Remove** the `/admin/wallet/:path*` `walletCsp` entry entirely (no Stripe in backoffice). Keep `standardCsp` only. Verify no `js.stripe.com` appears anywhere in the file.
- `auth.config.ts` — swap all `KEYCLOAK_B2B_*` env var references to `KEYCLOAK_BACKOFFICE_*`. `cookies.sessionToken.name = '__Secure-tbe-backoffice.session-token'`. `PROTECTED_PREFIXES = ['/'] ` (entire portal is staff-only).
- `lib/auth.ts` — same env var swap; identical Node-runtime session/refresh logic.
- `lib/api-client.ts` — byte-for-byte copy (no change needed; reads generic `GATEWAY_URL` + `session.access_token`).
- `lib/rbac.ts` — NEW. Export `isOpsAdmin(session)`, `isOpsCs(session)`, `isOpsFinance(session)`, `isOpsRead(session)` predicates reading `session.roles: string[]`.
- `middleware.ts` — mirror b2b-web but role-gate per UI-SPEC and PATTERNS.md Pattern M line 567:
  - `/approvals/*`, `/finance/wallet-credits/*`, `/operations/dlq/*`, `/bookings/cancellations/*` → require `ops-admin` OR (for read) `ops-finance`/`ops-cs` as applicable. For strictness: ops-admin only on `/approvals` and `/finance/wallet-credits` mutation routes. Read access: the RSC page itself is fine for ops-finance + ops-admin on wallet-credits, for ops-admin on approvals (queue-owner is ops-admin).
  - `/bookings/new` → require `ops-cs` or `ops-admin` (Plan 06-02 uses this).
  - `/customers/{id}/erase` → require `ops-admin`.
  - All other authenticated paths pass through. Non-authenticated → `/login`.
- `types/auth.d.ts` — declare `Session.roles: string[]`, `Session.user.ops_role: 'ops-admin' | 'ops-cs' | 'ops-finance' | 'ops-read'` for convenience.
- `types/ui.d.ts` — ambient shim mirroring b2b-web (Pitfall 17 — UI is `.jsx`).
- `components/ui/**` — copy UNCHANGED from `src/portals/b2b-web/components/ui/**` (Pitfall 17). Verify `diff -r src/portals/b2b-web/components/ui src/portals/backoffice-web/components/ui` exits 0.
- `components/layout/BackofficePortalBadge.tsx` — mirror `b2b-web/components/layout/agent-portal-badge.tsx` exactly, swap `indigo-600` → `slate-900`, `indigo-400` → `slate-200`, label `Agent portal` → `BACKOFFICE`, `aria-label` to match.
- `components/layout/FourEyesApprovalBadge.tsx` — NEW. Amber chip (`bg-amber-50 text-amber-900 border-amber-600`) with `role="status"` (NOT `role="alert"` per UI-SPEC), rendered when pending approvals exist for the current viewer.
- `app/(portal)/layout.tsx` — copy b2b-web's but: remove `<LowBalanceBanner />`; add `<BackofficePortalBadge />`, role chip derived from session, global search placeholder div, 4-eyes approval badge, DLQ/approvals/reconciliation count badges (counts land in Task 3/5/6).

**Keycloak realm MUST be imported** (user_setup gate) before the portal can authenticate; integration tests use a seeded Keycloak container fixture or mocked claims (NSubstitute per Plan 05-03 deviation).

Run `dotnet build src/services/BackofficeService/BackofficeService.API/BackofficeService.API.csproj` — must succeed. Run `pnpm --filter ./src/portals/backoffice-web install && pnpm --filter ./src/portals/backoffice-web run build` — must succeed. Run `dotnet ef migrations list --project src/services/BackofficeService/BackofficeService.Infrastructure` — must show the three new migrations.
  </action>
  <verify>
    <automated>dotnet build src/services/BackofficeService/BackofficeService.API/BackofficeService.API.csproj --no-restore &amp;&amp; dotnet build src/gateway/TBE.Gateway/TBE.Gateway.csproj --no-restore &amp;&amp; pnpm --filter ./src/portals/backoffice-web run build</automated>
  </verify>
  <done>
BackofficeService boots with 4 policies; gateway routes /api/backoffice/* with Backoffice scheme; realm JSON has 4 ops-* roles and tbe-api audience mapper; backoffice-web builds with basePath /backoffice, no Stripe CSP, slate-900 badge, 4-role middleware gates; 3 EF migrations present; components/ui/ is byte-identical to b2b-web (diff -r clean).
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: BO-09/BO-10 — _error queue consumer + DlqController + portal page</name>
  <files>
    src/services/BackofficeService/BackofficeService.Application/Consumers/ErrorQueueConsumer.cs,
    src/services/BackofficeService/BackofficeService.Application/Controllers/DlqController.cs,
    src/services/BackofficeService/BackofficeService.Application/Entities/DeadLetterQueueRow.cs,
    src/portals/backoffice-web/app/(portal)/operations/dlq/page.tsx,
    src/portals/backoffice-web/app/api/dlq/route.ts,
    src/portals/backoffice-web/app/api/dlq/[id]/requeue/route.ts,
    src/portals/backoffice-web/app/api/dlq/[id]/resolve/route.ts,
    tests/TBE.BackofficeService.Tests/DeadLetterQueueTests.cs
  </files>
  <behavior>
- Test: A message published to a receive endpoint whose consumer throws → retries are exhausted by MassTransit → message lands on `{queue}_error` → ErrorQueueConsumer persists a DeadLetterQueueRow with MessageType, OriginalQueue, Payload (full JSON), FailureReason (first 1000 chars of `MT-Fault-Message` header), FirstFailedAt, CorrelationId, RequeueCount=0, ResolvedAt=null.
- Test: `POST /api/backoffice/dlq/{id}/requeue` re-publishes the envelope to the live queue using `PublishContextCallback` that restores `MessageId`, `CorrelationId`, and all headers (RESEARCH anti-pattern 3). Increments `RequeueCount` and sets `LastRequeuedAt`. Returns 204.
- Test: `POST /api/backoffice/dlq/{id}/resolve` with body `{ reason }` sets `ResolvedAt=SYSUTCDATETIME()`, `ResolvedBy=preferred_username`, `ResolutionReason=reason`. Returns 204.
- Test: `GET /api/backoffice/dlq?status=unresolved&from=...&to=...` returns paged list (page size 20), filtered by `ResolvedAt IS NULL` when status=unresolved, sorted by FirstFailedAt DESC.
- Test: `BackofficeReadPolicy` required for GET; `BackofficeAdminPolicy` required for requeue/resolve.
- Test: Missing `preferred_username` claim on requeue/resolve → 401 problem+json (Pitfall 28 fail-closed).
  </behavior>
  <action>
**A. DeadLetterQueueRow entity** (`Entities/DeadLetterQueueRow.cs`): POCO matching the D-58 schema from `<interfaces>`. No behaviour; EF maps it via data-annotations or explicit `HasKey` in `BackofficeDbContext.OnModelCreating`.

**B. ErrorQueueConsumer** (`Consumers/ErrorQueueConsumer.cs`): implement `IConsumer<JsonObject>` (System.Text.Json.Nodes). Extract headers `MT-Fault-Message`, `MT-Fault-InputAddress`, `MT-MessageType` via `context.Headers.Get<string>(...)`. Build `DeadLetterQueueRow` with `Id = Guid.NewGuid()`, `MessageId = context.MessageId ?? Guid.NewGuid()`, `CorrelationId = context.CorrelationId`, `Payload = context.Message.ToJsonString()`, truncate `FailureReason` to 1000 chars, `FirstFailedAt = DateTime.UtcNow`. `_db.DeadLetterQueue.Add(row)` + `_db.SaveChangesAsync(context.CancellationToken)`. Inject `BackofficeDbContext` via constructor. Log at `Information` level: `"dlq-capture {Queue} {MessageType} {MessageId}"`. No throws — swallow any persistence failure with `LogError` so `_error` queue doesn't infinite-loop.

**C. DlqController** (`Controllers/DlqController.cs`): mirror `PaymentService.API.Controllers.WalletController.B2BWalletController` class shell per PATTERNS.md Pattern G. Route `[Route("api/backoffice/dlq")]`, `[Authorize(Policy = "BackofficeReadPolicy")]` on class (per-action overrides admin). Endpoints:
- `GET "/"` → paged query: `status` (unresolved | resolved | all, default unresolved), `from` datetime, `to` datetime, `page` int (1-based), `pageSize` (default 20, max 100). Returns `{ rows: [...], totalCount, page, pageSize }`. Rows include full schema + a `Preview` field (first 80 chars of FailureReason).
- `GET "/{id:guid}"` → full row with full `Payload` JSON (no truncation).
- `POST "/{id:guid}/requeue"` [Authorize(Policy = "BackofficeAdminPolicy")] → load row; extract `OriginalQueue`; via `ISendEndpointProvider.GetSendEndpoint(new Uri($"queue:{OriginalQueue}"))` send the deserialised `JsonObject` payload with `PublishContextCallback` restoring MessageId + CorrelationId + headers. Then `row.RequeueCount++; row.LastRequeuedAt = DateTime.UtcNow;` + SaveChangesAsync. Return 204.
- `POST "/{id:guid}/resolve"` [Authorize(Policy = "BackofficeAdminPolicy")] body `{ reason: string (min 1, max 500) }` → set `ResolvedAt`, `ResolvedBy = preferred_username`, `ResolutionReason = reason`. Return 204.
- Actor extraction: use the fail-closed pattern from `InvoicesController.cs` lines 48-54 — `var actor = User.FindFirst("preferred_username")?.Value; if (string.IsNullOrEmpty(actor)) return Unauthorized(new { error = "missing preferred_username claim" });` (return 401 with problem+json).

**D. backoffice-web DLQ page** (`app/(portal)/operations/dlq/page.tsx`): RSC that fetches `GET /api/backoffice/dlq?status=unresolved` via `gatewayFetch`. Renders:
- Filter bar: status tabs (Unresolved | Resolved | All), date-range picker, refreshed via `useRouter().push` (URL-driven per D-12 pattern).
- Table with columns per UI-SPEC §DLQ: Status dot (slate-400 resolved / red-600 unresolved), Message type chip (mono), Original queue (mono), First failed at (tabular), Requeue count badge, Failure reason (truncate 80 chars with title attribute for full text), Actions.
- Action buttons: `View envelope` (opens Dialog with `react-json-tree`), `Requeue` (confirmation dialog → POST to `/api/dlq/{id}/requeue`), `Resolve` (reason textarea required → POST to `/api/dlq/{id}/resolve`).
- TanStack Query: 60s refetchInterval, 45s staleTime (per PATTERNS.md Pattern O delta).
- Compact table (h-11 rows per UI-SPEC §Spacing scale).

**E. Route handlers** (3 files under `app/api/dlq/`): thin Node-runtime proxies that call `gatewayFetch`. Follow the pattern in `src/portals/b2b-web/app/api/wallet/threshold/route.ts` (the Plan 05-05 HI-01 fix). Stream responses via `new Response(upstream.body, { status, headers })` (Pitfall 11+14).

**F. Integration test** (`DeadLetterQueueTests.cs`) — `[Collection("Phase06Sql")]` + `[Collection("Phase06Rabbit")]`. Replace the red placeholder with a real test:
1. Arrange: `await using var host = ...` bootstraps a WebApplicationFactory with `BackofficeService.Program` + Testcontainers SQL + Testcontainers RabbitMQ; register a test consumer `ThrowingConsumer` on a `test-throw` receive endpoint. Configure `UseMessageRetry(r => r.None())` so faults arrive on `_error` immediately.
2. Act: `IPublishEndpoint.Publish(new ThrowingMessage("boom"))` → wait for MassTransit test harness `IConsumed<>` on ErrorQueueConsumer.
3. Assert: `db.DeadLetterQueue.Single(...).Payload` contains `"boom"`; FailureReason contains the thrown message; RequeueCount==0.
4. Act: call `DlqController.Requeue(row.Id)` with a seeded `preferred_username` claim via `TestAuthHandler`.
5. Assert: new message arrives on `test-throw` queue preserving `MessageId` and `CorrelationId`; `row.RequeueCount == 1`, `LastRequeuedAt != null`.
6. Act: call `DlqController.Resolve(row.Id, { reason = "triaged — known flake" })`.
7. Assert: `ResolvedAt != null`, `ResolutionReason == "triaged — known flake"`, `ResolvedBy == "ops-admin-1"`.

Test class: `DeadLetterQueueTests` (matches VALIDATION filter `FullyQualifiedName~DeadLetterQueueTests`). Single `[Fact]` orchestrates the full scenario. Remove `[Trait("Category","RedPlaceholder")]` trait; keep `[Trait("Category","Phase06")]`.
  </action>
  <verify>
    <automated>dotnet test tests/TBE.BackofficeService.Tests/TBE.BackofficeService.Tests.csproj --filter "FullyQualifiedName~DeadLetterQueueTests" --no-restore</automated>
  </verify>
  <done>
ErrorQueueConsumer persists dead letters with headers intact; DlqController exposes list/get/requeue/resolve with correct role gates and fail-closed actor extraction; requeue preserves MessageId + CorrelationId; portal page renders with 60s poll, envelope viewer, and role-gated actions; DeadLetterQueueTests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: BO-04/BO-05 — BookingEvents append-only table, role-grant enforcement, BookingEventsWriter wired into saga</name>
  <files>
    src/services/BookingService/BookingService.Infrastructure/Migrations/20260601100000_AddBookingEventsTable.cs,
    src/services/BookingService/BookingService.Infrastructure/Migrations/20260601100001_AddAppendOnlyRoleGrants.cs,
    src/services/BookingService/BookingService.Infrastructure/Migrations/20260601100002_AddCancellationColumns.cs,
    src/services/BookingService/BookingService.Infrastructure/BookingEventsDbContext.cs,
    src/services/BookingService/BookingService.Application/Saga/BookingEvent.cs,
    src/services/BookingService/BookingService.Application/BookingEventsWriter.cs,
    src/services/BookingService/BookingService.API/Program.cs,
    tests/TBE.BackofficeService.Tests/BookingEventsAppendOnlyTests.cs,
    tests/TBE.BackofficeService.Tests/BookingEventsSnapshotTests.cs
  </files>
  <behavior>
- Test (BO-04 / D-49): `UPDATE dbo.BookingEvents SET Actor='x' WHERE EventId=@id` executed as SQL user `tbe_booking_app` throws `SqlException` with error number 229 or 230 (`DENY permission`). `DELETE FROM dbo.BookingEvents WHERE EventId=@id` same.
- Test: `INSERT INTO dbo.BookingEvents (...)` succeeds as `tbe_booking_app`; `SELECT` succeeds.
- Test (BO-05): After a simulated saga confirm cycle, BookingEvents contains rows for each transition (BookingInitiated, PriceReconfirmed, PNRCreated, PaymentAuthorized, TicketIssued, PaymentCaptured, BookingConfirmed), each with a non-empty `Snapshot` JSON that parses and contains `BookingId`, `PricingBreakdown { BaseFareAmount, SurchargeAmount, TaxAmount, GrossAmount, AgencyNetFare?, MarkupAmount?, CommissionAmount? }`, `SupplierResponse { Pnr?, TicketNumber?, GdsRecordLocator? }`.
- Test: BookingEventsWriter uses a separate connection string (`BookingEventsWriter`) that maps to `tbe_booking_app` login — verified via `SELECT SUSER_SNAME()` during write path. Writer's DbContext does NOT contain `DbSet<BookingSagaState>` (compile-time guard against cross-DbSet ChangeTracker updates — Pitfall 1).
- Test: Down-migration drops the `booking_events_writer` role cleanly (no orphaned permissions).
  </behavior>
  <action>
**A. `BookingEvent` entity** (`Saga/BookingEvent.cs`): POCO — `EventId GUID PK`, `BookingId GUID`, `EventType nvarchar(64)`, `OccurredAt datetime2`, `Actor nvarchar(128)`, `CorrelationId GUID`, `Snapshot nvarchar(max)`. Constructor forces all fields (no setters — read-only once constructed).

**B. Migration `20260601100000_AddBookingEventsTable.cs`** — mirror PATTERNS.md Pattern D. Table `dbo.BookingEvents`, primary key `EventId`, non-unique index on `BookingId`, non-unique index on `(BookingId, OccurredAt)`, no outbox columns (writer-only context).

**C. Migration `20260601100001_AddAppendOnlyRoleGrants.cs`** — copy PATTERNS.md Pattern E verbatim. `migrationBuilder.Sql(@"IF NOT EXISTS (SELECT 1 FROM sys.database_principals WHERE name = 'booking_events_writer') CREATE ROLE booking_events_writer; GRANT INSERT, SELECT ON dbo.BookingEvents TO booking_events_writer; DENY UPDATE, DELETE ON dbo.BookingEvents TO booking_events_writer; IF NOT EXISTS (SELECT 1 FROM sys.database_role_members rm JOIN sys.database_principals p ON rm.member_principal_id = p.principal_id WHERE p.name = 'tbe_booking_app' AND rm.role_principal_id = (SELECT principal_id FROM sys.database_principals WHERE name='booking_events_writer')) ALTER ROLE booking_events_writer ADD MEMBER tbe_booking_app;");` Down: `DROP ROLE booking_events_writer;` after stripping grants.

Add a **fallback idempotent user creation block** at the top (tests use a fresh SQL container where `tbe_booking_app` login doesn't exist): `IF NOT EXISTS (SELECT 1 FROM sys.database_principals WHERE name='tbe_booking_app') CREATE USER tbe_booking_app WITHOUT LOGIN;` — for integration-test fixtures; production uses a real SQL login mapped via ops deploy.

**D. Migration `20260601100002_AddCancellationColumns.cs`** — add to `dbo.BookingSagaState`: `CancelledByStaff bit DEFAULT 0`, `CancellationReasonCode nvarchar(64) NULL CHECK (CancellationReasonCode IS NULL OR CancellationReasonCode IN ('CustomerRequest','SupplierInitiated','FareRuleViolation','FraudSuspected','DuplicateBooking','Other'))`, `CancellationReason nvarchar(500) NULL`, `CancellationRequestedBy nvarchar(128) NULL`, `CancellationApprovedBy nvarchar(128) NULL`, `CancellationApprovedAt datetime2 NULL`.

**E. `BookingEventsDbContext.cs`** — new DbContext per PATTERNS.md Pattern C+F. Constructor takes `DbContextOptions<BookingEventsDbContext>`. Single `DbSet<BookingEvent> Events`. `OnModelCreating` configures primary key + indexes; **does NOT call `AddInboxStateEntity/AddOutboxMessageEntity/AddOutboxStateEntity`** (Pitfall 1 — no ChangeTracker updates against DENY'd rows).

Register in `BookingService.API/Program.cs` with a distinct connection string: `builder.Services.AddDbContext<BookingEventsDbContext>(o => o.UseSqlServer(builder.Configuration.GetConnectionString("BookingEventsWriter") ?? builder.Configuration.GetConnectionString("Default")));` — fallback to `Default` in dev so testcontainers fixtures work without extra env var.

**F. `BookingEventsWriter.cs`** — `IBookingEventsWriter` interface + implementation. Single method: `Task WriteAsync(Guid bookingId, string eventType, string actor, Guid correlationId, object snapshotPayload, CancellationToken ct)`. Body: serialise `snapshotPayload` to JSON via `JsonSerializer.Serialize(snapshotPayload, new JsonSerializerOptions { WriteIndented = false })`, construct `BookingEvent`, `_db.Events.Add(evt); await _db.SaveChangesAsync(ct);`. Register as Scoped in Program.cs.

**G. Saga integration (BO-05)** — modify `BookingService.Application/Saga/BookingSaga.cs`. On every state transition (BookingInitiated, PriceReconfirmed, PNRCreated, PaymentAuthorized, TicketIssued, PaymentCaptured, BookingConfirmed, BookingCancelled), add `await _bookingEventsWriter.WriteAsync(...)` call **after the state transition but before the outbox publish**. Actor source:
- For saga auto-transitions: `"system:BookingSaga"`.
- For staff-initiated (Task 5 StaffBookingActionsController): actor flows through a new `ConsumeContext` header `X-Staff-Actor` (the publish-side stamps it from `preferred_username`).

Snapshot payload:
```csharp
new {
    BookingId = state.CorrelationId,
    Channel = state.Channel.ToString(),
    Status = state.Status,
    PricingBreakdown = new {
        state.BaseFareAmount, state.SurchargeAmount, state.TaxAmount, state.GrossAmount,
        state.AgencyNetFare, state.MarkupAmount, state.CommissionAmount
    },
    SupplierResponse = new { state.Pnr, state.TicketNumber, state.GdsRecordLocator }
}
```

**H. Tests**:

`BookingEventsAppendOnlyTests.cs` — replaces red placeholder. Steps:
1. Apply migrations against Testcontainers MsSql, connecting initially as `sa`.
2. Create a test SQL user `tbe_booking_app` (migration block handles CREATE USER WITHOUT LOGIN; alternative: `CREATE LOGIN tbe_booking_app_test WITH PASSWORD = 'Testing123!'; CREATE USER tbe_booking_app FOR LOGIN tbe_booking_app_test; EXEC sp_addrolemember 'booking_events_writer', 'tbe_booking_app';`).
3. Insert a row via raw ADO connection as `tbe_booking_app` — verify 1 row.
4. Attempt `UPDATE dbo.BookingEvents SET Actor='x'` — assert `SqlException` with Number 229 (permission denied).
5. Attempt `DELETE FROM dbo.BookingEvents` — assert same.
6. Verify SELECT succeeds.

`BookingEventsSnapshotTests.cs` — replaces red placeholder. Steps:
1. Apply migrations.
2. Use MassTransit test harness to drive `BookingSaga` through a full happy-path: publish `BookingInitiated`, `PriceReconfirmed`, `PnrCreated`, `PaymentAuthorized`, `TicketIssued`, `PaymentCaptured`, each with the saga publishing `BookingConfirmed` at the end.
3. Query `db.Events.Where(e => e.BookingId == id).OrderBy(e => e.OccurredAt).ToList()` — assert 7 rows (one per transition) with non-empty `Snapshot`.
4. For each row, parse Snapshot JSON and assert `BookingId`, `Channel`, `Status`, `PricingBreakdown.GrossAmount` present. For the `TicketIssued` row: assert `SupplierResponse.TicketNumber` non-null.
  </action>
  <verify>
    <automated>dotnet test tests/TBE.BackofficeService.Tests/TBE.BackofficeService.Tests.csproj --filter "FullyQualifiedName~BookingEventsAppendOnlyTests|FullyQualifiedName~BookingEventsSnapshotTests" --no-restore</automated>
  </verify>
  <done>
Three migrations apply; DENY role grant rejects UPDATE/DELETE at the SQL engine; BookingEventsDbContext is writer-only with no inbox/outbox maps; BookingEventsWriter is called on every saga transition with full pricing+supplier snapshot; BookingEventsAppendOnlyTests + BookingEventsSnapshotTests green.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Checkpoint: BookingEvents DENY migration applied — recompact before 4-eyes workflow</name>
  <files></files>
  <what-built>
Task above (BO-04/BO-05) applied three migrations to BookingService (AddBookingEventsTable, AddAppendOnlyRoleGrants, AddCancellationColumns), shipped BookingEventsDbContext (writer-only), BookingEventsWriter, and wired `_bookingEventsWriter.WriteAsync(...)` into every BookingSaga transition. Integration tests BookingEventsAppendOnlyTests + BookingEventsSnapshotTests are green — UPDATE/DELETE on dbo.BookingEvents is rejected at the SQL engine by the DENY role grant.

Purpose of this checkpoint: the next task (BO-03 staff cancel/modify + D-39 manual wallet credit) introduces a new subsystem (4-eyes state machine across 2 controllers + a cross-service MassTransit consumer). Executor should `/clear` or compact here so the BookingEvents plumbing just shipped doesn't consume context budget during the 4-eyes implementation.
  </what-built>
  <how-to-verify>
1. Run `dotnet test tests/TBE.BackofficeService.Tests/TBE.BackofficeService.Tests.csproj --filter "FullyQualifiedName~BookingEventsAppendOnlyTests|FullyQualifiedName~BookingEventsSnapshotTests" --no-restore` — both tests green.
2. Confirm `dotnet ef migrations list --project src/services/BookingService/BookingService.Infrastructure` shows `AddBookingEventsTable`, `AddAppendOnlyRoleGrants`, `AddCancellationColumns`.
3. Grep `grep -r "_bookingEventsWriter\.WriteAsync" src/services/BookingService/BookingService.Application/Saga/BookingSaga.cs` → 7+ matches (one per transition).
4. Verify no regression: `dotnet test tests/TBE.BookingService.Tests/ --no-restore` still green.
5. Commit the three-task bundle (Wave 0, bootstrap, DLQ, BookingEvents) before starting 4-eyes work — `git log --oneline` shows clean checkpoints.
  </how-to-verify>
  <resume-signal>Type "approved" to proceed with 4-eyes controllers and cross-service wallet-credit consumer, or describe issues to address first.</resume-signal>
  <action>
This is a human-verification checkpoint (type="checkpoint:human-verify"). The executor MUST pause here and wait for the user to run the steps inside `<how-to-verify>` and reply with the `<resume-signal>`. No Claude-side action beyond rendering the checkpoint to the user. See `<what-built>` for the scope just completed and `<how-to-verify>` for the verification steps. Do NOT proceed to the next task until the user approves.
  </action>
  <verify>
    <automated>echo "Manual checkpoint — user verification required. See &lt;how-to-verify&gt; block above."; exit 0</automated>
  </verify>
  <done>
User has typed "approved" (or equivalent) per the `<resume-signal>` contract, confirming the scope described in `<what-built>` is green and context can be recompacted before the next task.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 6: BO-03 staff cancel/modify + D-39 manual wallet credit — 4-eyes controllers, PaymentService consumer, portal pages</name>
  <files>
    src/services/BackofficeService/BackofficeService.Application/Controllers/StaffBookingActionsController.cs,
    src/services/BackofficeService/BackofficeService.Application/Controllers/WalletCreditRequestsController.cs,
    src/services/PaymentService/PaymentService.Application/Wallet/WalletCreditApprovedConsumer.cs,
    src/services/PaymentService/PaymentService.Infrastructure/Migrations/20260601200000_AddManualCreditKind.cs,
    src/shared/TBE.Contracts/Events/BackofficeEvents.cs,
    src/portals/backoffice-web/app/(portal)/bookings/cancellations/page.tsx,
    src/portals/backoffice-web/app/(portal)/finance/wallet-credits/page.tsx,
    src/portals/backoffice-web/app/api/bookings/[id]/cancel/route.ts,
    src/portals/backoffice-web/app/api/bookings/[id]/cancel/approve/route.ts,
    src/portals/backoffice-web/app/api/wallet-credits/route.ts,
    src/portals/backoffice-web/app/api/wallet-credits/[id]/approve/route.ts,
    tests/TBE.BackofficeService.Tests/StaffCancelModifyTests.cs,
    tests/TBE.BackofficeService.Tests/ManualWalletCreditFourEyesTests.cs
  </files>
  <behavior>
**BO-03 Staff cancel/modify (4-eyes per D-48):**
- Test: `POST /api/backoffice/bookings/{id}/cancel` body `{ reasonCode, reason }` as ops-cs → creates `CancellationRequest` with Status=PendingApproval, ExpiresAt = now+72h. Returns 202 Accepted with request id.
- Test: `POST /api/backoffice/bookings/{id}/cancel/approve` body `{ approvalReason }` as ops-admin (different `preferred_username` from RequestedBy) → flips status to Approved, publishes `BookingCancellationApproved` via IPublishEndpoint inside EF outbox transaction, which the existing `BookingService` compensation pipeline consumes per Plan 03-01. Writes one BookingEvents row with EventType='BookingCancellationApproved' and Actor=approver. Returns 204.
- Test: Same actor tries to approve their own request → 403 with problem+json `type=/errors/four-eyes-self-approval`.
- Test: Approval of already-approved/denied request → 409 with problem+json `type=/errors/four-eyes-already-decided`.
- Test: Approval after ExpiresAt → 409 `type=/errors/four-eyes-expired`.
- Test: Non-admin caller on approve → 403 from policy layer (AuthZ).

**D-39 Manual wallet credit (4-eyes):**
- Test: `POST /api/backoffice/wallet-credits` as ops-finance body `{ agencyId, amount, currency, reasonCode, linkedBookingId?, notes }` → creates WalletCreditRequest Status=PendingApproval, ExpiresAt = now+72h. Amount validated against range (0.01 – 100000). ReasonCode validated against D-53 enum. Returns 202.
- Test: `POST /api/backoffice/wallet-credits/{id}/approve` as ops-admin (different user) body `{ approvalNotes }` → flips status to Approved, publishes `WalletCreditApproved` via IPublishEndpoint inside outbox.
- Test: `WalletCreditApprovedConsumer` in PaymentService consumes the event and writes `payment.WalletTransactions` with `Kind = 'ManualCredit'`, positive `SignedAmount = amount`, `WalletId = agencyId`, `ApprovedBy = approver username`, `ApprovalNotes`. Idempotent via MassTransit outbox InboxState dedup on MessageId.
- Test: Self-approval → 403 `type=/errors/four-eyes-self-approval`.
- Test: Expired approval → 409 `type=/errors/four-eyes-expired`.
  </behavior>
  <action>
**A. `BackofficeEvents.cs`** (`src/shared/TBE.Contracts/Events/`) — add the 3 records from `<interfaces>` (WalletCreditRequested, WalletCreditApproved, BookingCancelledByStaff). Add `BookingCancellationApproved(Guid BookingId, string ReasonCode, string Reason, string RequestedBy, string ApprovedBy, string ApprovalReason, DateTime ApprovedAt)` as a saga-consumable event.

**B. `StaffBookingActionsController`** per PATTERNS.md Pattern G + H. Route `[Route("api/backoffice/bookings/{bookingId:guid}/cancel")]`. Endpoints:
- `POST ""` [Authorize(Policy = "BackofficeCsPolicy")] body `CancelBookingReq { string ReasonCode, string Reason }`. Validate ReasonCode in the 6-value enum. Create `CancellationRequest { Id=Guid.NewGuid(), BookingId, ReasonCode, Reason, RequestedBy=preferred_username, RequestedAt=now, ExpiresAt=now.AddHours(72), Status="PendingApproval" }`. SaveChanges. Return 202 with `{ id }`.
- `POST "/approve"` [Authorize(Policy = "BackofficeAdminPolicy")] body `ApproveReq { string ApprovalReason }`. Self-approval guard + conflict/expiry checks per PATTERNS.md Pattern H. On success: Status="Approved", ApprovedBy=approver, ApprovedAt=now. `await _publish.Publish(new BookingCancellationApproved(...))`. `await _bookingEventsWriter.WriteAsync(bookingId, "BookingCancellationApproved", approver, correlationId, snapshot)`. `await _db.SaveChangesAsync(ct)` (outbox ensures atomic publish+row-write). Return 204.
- `POST "/deny"` [Authorize(Policy = "BackofficeAdminPolicy")] body `DenyReq { string Reason }` — flips to Denied, writes BookingEvents row, no publish. Return 204.

Inject `BackofficeDbContext`, `IPublishEndpoint`, `IBookingEventsWriter` (cross-project reference — BackofficeService.Application references BookingService.Application for the writer interface; or define `IBookingEventsWriter` in `TBE.Common` and put implementation in BookingService.Application. **Chosen path (lower coupling):** move `IBookingEventsWriter` interface to `src/shared/TBE.Common/BookingAudit/IBookingEventsWriter.cs`; BackofficeService references TBE.Common). Actor via `User.FindFirst("preferred_username")`.

**C. `WalletCreditRequestsController`** mirrors StaffBookingActionsController. Route `[Route("api/backoffice/wallet-credits")]`:
- `POST ""` [Authorize(Policy = "BackofficeFinancePolicy")] body `CreateReq { Guid AgencyId, decimal Amount, string Currency, string ReasonCode, Guid? LinkedBookingId, string Notes }`. Validate Amount ∈ [0.01, 100000]; ReasonCode in D-53 enum. 400 problem+json `type=/errors/wallet-credit-invalid-amount` or `/errors/wallet-credit-invalid-reason` on violation. Create row. Return 202 with id.
- `POST "/{id}/approve"` [Authorize(Policy = "BackofficeAdminPolicy")] body `ApproveReq { string ApprovalNotes }`. Self-approval guard + expiry + conflict guards. Flip status, set ApprovedBy/ApprovedAt/ApprovalNotes. `await _publish.Publish(new WalletCreditApproved(...))`. SaveChanges. Return 204.
- `POST "/{id}/deny"` [BackofficeAdminPolicy] → Denied, no publish.
- `GET "/"` [BackofficeReadPolicy but ops-finance + ops-admin only in practice; enforce with a tighter policy `RequireRole("ops-finance","ops-admin")` inline] → paged list filtered by status.

**D. PaymentService migration `20260601200000_AddManualCreditKind.cs`** — extend `payment.WalletTransactions.Kind` CHECK constraint (or whatever current constraint exists) to include `'ManualCredit'` and `'CommissionPayout'` (D-54 adds the second; include here since Plan 06-03 needs it too). Add nullable columns `ApprovedBy nvarchar(128) NULL`, `ApprovalNotes nvarchar(500) NULL`. Down-migration reverts constraint and drops columns.

**E. `WalletCreditApprovedConsumer.cs`** (PaymentService) — implement `IConsumer<WalletCreditApproved>`. Use the existing `AgencyWalletRepository` pattern (raw Dapper `MERGE`/`UPDLOCK` per D-15 + D-14 append-only). Build a `WalletTransaction` with `Kind='ManualCredit'`, `SignedAmount=+amount`, `WalletId=agencyId`, `RelatedBookingId=linkedBookingId`, `ApprovedBy`, `ApprovalNotes`, `CreatedAt=now`. Insert via the repository's append-only path. MassTransit outbox InboxState dedup handles idempotency (MessageId uniqueness prevents double-credit if the consumer runs twice after a crash — Plan 03-01 pattern).

Register the consumer in `PaymentService.API/Program.cs` `AddTbeMassTransitWithRabbitMq(configureConsumers: x => { ... x.AddConsumer<WalletCreditApprovedConsumer>(); })`.

**F. Portal pages:**
- `app/(portal)/bookings/cancellations/page.tsx` — ops-admin queue. Tabs: Pending | Approved | Denied | Expired. Table with compact h-11 rows per UI-SPEC. Approve/Deny dialogs with reason textarea required.
- `app/(portal)/finance/wallet-credits/page.tsx` — ops-finance + ops-admin view. `New credit request` button opens dialog: AgencyTypeahead, Amount input (£ symbol, 2-decimal), ReasonCode radio group (4 D-53 values), LinkedBooking typeahead (optional), Notes required. Approval dialog (ops-admin) has approval-notes textarea + self-approval guard rendered as 403 inline error when caught.
- 4 route handler files (`app/api/bookings/[id]/cancel/route.ts` POST, `/cancel/approve/route.ts` POST, `app/api/wallet-credits/route.ts` GET+POST, `/[id]/approve/route.ts` POST) — thin Node-runtime proxies using `gatewayFetch` per Plan 05-01 pattern.

**G. Tests:**

`StaffCancelModifyTests.cs` — 4-test scenario in a single `[Fact]` method (or split into Theory cases):
1. ops-cs opens cancel → row created, 202 returned.
2. ops-admin (different user) approves → 204, row flipped, publish captured by harness, BookingEvents row written.
3. Same actor approve own request → 403 problem+json with `four-eyes-self-approval` type.
4. Approve after ExpiresAt rewound → 409 `four-eyes-expired`.

`ManualWalletCreditFourEyesTests.cs` — mirrors StaffCancelModifyTests structure:
1. ops-finance creates request → 202.
2. ops-admin approves → 204; harness captures `WalletCreditApproved`.
3. `WalletCreditApprovedConsumer` writes `WalletTransactions` row with Kind=ManualCredit, ApprovedBy, atomic (BalanceBefore=X, BalanceAfter=X+amount).
4. Duplicate delivery (same MessageId) → second write NOT inserted (inbox dedup) — query `db.WalletTransactions.Count(...)` stays at 1.
5. Self-approval → 403.
6. Invalid reason code → 400 problem+json.
  </action>
  <verify>
    <automated>dotnet test tests/TBE.BackofficeService.Tests/TBE.BackofficeService.Tests.csproj --filter "FullyQualifiedName~StaffCancelModifyTests|FullyQualifiedName~ManualWalletCreditFourEyesTests" --no-restore</automated>
  </verify>
  <done>
Both 4-eyes flows ship; self-approval blocked at both UI and server layer; expired requests rejected; approval atomic with EF outbox (publish + row-write committed together); PaymentService consumes WalletCreditApproved and writes Kind=ManualCredit idempotently; BookingCancellationApproved is consumed by existing Plan 03-01 saga compensation pipeline; portal pages role-gated at middleware layer; both tests green.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 7: BO-01 unified booking list — BookingsController + portal page + RBAC filter</name>
  <files>
    src/services/BackofficeService/BackofficeService.Application/Controllers/BookingsController.cs,
    src/portals/backoffice-web/app/(portal)/bookings/page.tsx,
    src/portals/backoffice-web/app/(portal)/bookings/[id]/page.tsx,
    src/portals/backoffice-web/app/api/bookings/route.ts,
    tests/TBE.BackofficeService.Tests/UnifiedBookingListTests.cs
  </files>
  <behavior>
- Test: `GET /api/backoffice/bookings?page=1&pageSize=20` as ops-read returns paged rows from `dbo.BookingSagaState` spanning Channel=B2C (0), B2B (1), Manual (2 once Plan 06-02 ships but column exists now per D-56 enum), with fields: BookingId, Channel chip-ready string, Status, PNR, CustomerName/Email OR AgencyName (mutually exclusive), GrossAmount, Currency, CreatedAt.
- Test: Filter by `channel=B2C` returns only Channel=0 rows.
- Test: Filter by `status=Confirmed` returns only confirmed rows.
- Test: Filter by `q=PNR123` fuzzy-matches PNR, CustomerName, CustomerEmail, BookingReference.
- Test: Response does NOT include agency_id filtering (backoffice staff see all agencies; no tenant scoping like the B2B portal's agency_id filter).
- Test: All 4 ops-* roles can call GET. Non-authenticated → 401.
- Test: Cross-tenant 404 pitfall (Pitfall 10) is NOT applicable here — backoffice staff see everything.
- Test: `GET /api/backoffice/bookings/{id}` returns a detail response including BookingEvents timeline.
  </behavior>
  <action>
**A. `BookingsController.cs`** — Route `[Route("api/backoffice/bookings")]`, `[Authorize(Policy = "BackofficeReadPolicy")]`. Cross-service data: BackofficeService reads BookingService's DB via a **read-only connection string** OR by adding a DbSet to BackofficeDbContext that points at the `booking` schema (not the `backoffice` schema). **Chosen path: cross-schema read via BackofficeDbContext**. Add `DbSet<BookingSagaStateReadModel> BookingReadModel` where `BookingSagaStateReadModel` is a POCO mapped to `dbo.BookingSagaState` in `OnModelCreating` using `.ToTable("BookingSagaState", "dbo")`. (Rationale: single DB per deployment target in dev — cross-service read is a known acceptable pattern per CONTEXT §Integration Points; production deploy may split DBs, in which case this becomes a projection that Plan 06-04 CRM delivers.)

Endpoints:
- `GET ""` query params `page` (1-based, default 1), `pageSize` (default 20, max 100), `channel` ('B2C' | 'B2B' | 'Manual'), `status` (Confirmed | Pending | Failed | Cancelled | ...), `q` (free text), `from`, `to`. Returns:
  ```json
  { "rows": [{ "bookingId", "channel", "status", "pnr", "ticketNumber", "customerName", "customerEmail", "agencyId", "agencyName", "grossAmount", "currency", "createdAt", "confirmedAt", "cancelledAt" }], "totalCount", "page", "pageSize" }
  ```
  LINQ: `.Where(b => (channel == null || b.Channel == parsedChannel) && (status == null || b.Status == status) && (q == null || EF.Functions.Like(b.Pnr, $"%{q}%") || EF.Functions.Like(b.CustomerName, $"%{q}%") || EF.Functions.Like(b.CustomerEmail, $"%{q}%") || EF.Functions.Like(b.BookingReference, $"%{q}%")))`.
  **AgencyName lookup:** Phase 6 Plan 06-04 ships CRM Agencies projection. For this plan, return `AgencyId` only; portal resolves name via Plan 06-04's endpoint (placeholder: render AgencyId truncated as chip with tooltip).

- `GET "/{id:guid}"` returns detail: full BookingSagaState fields + BookingEvents timeline (query `bookingEventsDb.Events.Where(e => e.BookingId == id).OrderBy(e => e.OccurredAt).ToList()`) + CancellationRequests + (if any) ManualBooking metadata (Plan 06-02 wires it). Inject both `BackofficeDbContext` and `BookingEventsDbContext`.

**B. Portal pages:**

`app/(portal)/bookings/page.tsx` — RSC that calls `gatewayFetch('/api/backoffice/bookings?' + params)`. Renders per UI-SPEC §Unified Booking List:
- Compact table h-11 rows.
- Columns: Channel chip (B=blue, A=indigo, M=slate with `edit-3` icon per UI-SPEC), PNR (mono + copy-to-clipboard), Customer/Agency, Total (tabular-nums), Status chip, Created at (relative), Actions (View).
- Filter bar: channel multi, status multi, date-range, free-text search.
- Paging: 20/50/100 with page numbers per Plan 05-04 D-44 precedent.
- `useSearchParams` + `useRouter` URL-driven filters (Plan 04-02 D-12 pattern).

`app/(portal)/bookings/[id]/page.tsx` — detail RSC. Tabs: Overview | Audit log | Pricing snapshot | Actions. Audit log tab renders BookingEvents timeline (h-9 dense rows per UI-SPEC §Spacing — the ONE exception to h-11). Pricing snapshot tab renders the latest snapshot JSON via `react-json-tree`. Actions tab shows `Request cancellation` button (ops-cs + ops-admin), `Request modification` (Phase 7 placeholder — disabled for now with tooltip), `Erase customer PII` (ops-admin, links to `/customers/{customerId}/erase` — page lands in Plan 06-04).

`app/api/bookings/route.ts` — Node-runtime GET proxy via `gatewayFetch` per Plan 05-01 pattern. Stream response.

**C. Test `UnifiedBookingListTests.cs`** — replaces red placeholder. Arrange: seed 3 bookings (1 B2C, 1 B2B, 1 Manual by raw INSERT with Channel=2 since Plan 06-02 enum ships Manual — if enum migration not yet applied, this test seeds with `Channel=0` or `1` only and asserts 2 rows; updated post-Plan 06-02). Act: call controller.GetList with each ops-* role via TestAuthHandler. Assert:
- All 4 roles get 200 and see all rows.
- Channel filter narrows correctly.
- Status filter narrows correctly.
- Pagination respects pageSize.
- 401 when no auth.
- Detail endpoint returns empty `bookingEvents` array for an unconfirmed booking; returns populated array after invoking BookingEventsWriter directly.
  </action>
  <verify>
    <automated>dotnet test tests/TBE.BackofficeService.Tests/TBE.BackofficeService.Tests.csproj --filter "FullyQualifiedName~UnifiedBookingListTests" --no-restore</automated>
  </verify>
  <done>
GET /api/backoffice/bookings returns cross-channel paged list with filters; GET /{id} returns detail including BookingEvents timeline; portal list + detail pages render with compact tables, URL-driven filters, and role-gated action buttons; test passes with all 4 ops-* roles + all filters covered.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser → /backoffice (edge) | Auth.js session verification + role gates in middleware.ts |
| browser → backoffice-web route handlers (Node) | Session-bound Bearer forwarding via `gatewayFetch` |
| backoffice-web → YARP gateway | JWT scheme `Backoffice` validates issuer + audience + lifetime |
| YARP → BackofficeService | Gateway pins `BackofficePolicy`; service re-validates JWT and applies `BackofficeReadPolicy`/`CsPolicy`/`FinancePolicy`/`AdminPolicy` |
| BackofficeService → RabbitMQ | MassTransit with TLS + outbox pattern (inherited Phase 1 config) |
| BackofficeService → SQL Server (backoffice schema) | Default SQL login |
| BookingEventsWriter → SQL Server (dbo.BookingEvents) | **Separate SQL login `tbe_booking_app`**, member of `booking_events_writer` role (GRANT INSERT,SELECT; DENY UPDATE,DELETE) |
| BackofficeService → BookingService DB (read-only via BackofficeDbContext cross-schema read) | Shared deployment target in dev; prod may split via projection (Plan 06-04 CRM provides the canonical cross-service read path) |

## STRIDE Threat Register (ASVS L1)

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-6-01 | Tampering | BookingEvents table | mitigate | DENY UPDATE,DELETE at SQL engine (D-49 / Pattern E). Verified by `BookingEventsAppendOnlyTests`. Anti-pattern 2 guarded via separate BookingEventsDbContext. |
| T-6-02 | Elevation of Privilege | Staff cancel/modify | mitigate | `BackofficeCsPolicy` gates open; `BackofficeAdminPolicy` gates approve. Self-approval guard in PATTERNS.md Pattern H (403 `four-eyes-self-approval`). Approval state machine has 72h ExpiresAt (anti-pattern 5). |
| T-6-03 | Tampering / Repudiation | Manual wallet credit | mitigate | `BackofficeFinancePolicy` for open, `BackofficeAdminPolicy` for approve. Self-approval guard. `Amount ∈ [0.01, 100000]` CHECK constraint AND controller-level 400 problem+json. D-53 ReasonCode locked as CHECK enum. Audit via BookingEvents writer + ApprovedBy on WalletTransactions row. |
| T-6-04 | DoS / Spoofing | DLQ requeue | mitigate | `BackofficeAdminPolicy` on requeue/resolve. Requeue uses `PublishContextCallback` to restore MessageId + CorrelationId (anti-pattern 3). Infinite-loop guard: ErrorQueueConsumer swallows persistence failures with LogError so `_error` queue does not requeue itself. |
| T-6-05 | Info Disclosure | Cross-tenant booking list | accept | Backoffice staff are authorised to see all agencies + customers — this is the backoffice's purpose. No agency_id filter. However, **ops-read** MUST NOT see BookingEvents with Snapshot fields containing unredacted PII when Plan 06-04 GDPR erasure lands; Plan 06-04 adds PII-replace-on-read to the projection layer. |
| T-6-06 | Auth bypass | backoffice portal → /forbidden path | mitigate | middleware.ts role-gates every route. Non-ops-* authenticated users redirected to `/forbidden`. Unauthenticated to `/login`. Cookie scoped `__Secure-tbe-backoffice.session-token` (Pitfall 19). |
| T-6-07 | Audience confusion | Gateway Backoffice scheme | mitigate | `ValidateAudience = true` with `Audience = "tbe-api"`. Every `BackofficePolicy` calls `.AddAuthenticationSchemes("Backoffice")` pin (Pitfall 4 echo from Plan 05-01). |
| T-6-08 | Info disclosure via error response | 4-eyes/wallet-credit failures | mitigate | RFC-7807 problem+json without stack traces or SQL state. Type URIs enumerated in PATTERNS.md Pattern G. 500-class errors logged internally only. |
| T-6-09 | CSRF on mutation routes | backoffice-web route handlers | mitigate | Auth.js v5 cookie sameSite=lax + Bearer forwarding requires session. Route handlers are Node-runtime only — no client-rendered forms can bypass. |
| T-6-10 | Replay / double-credit | WalletCreditApprovedConsumer | mitigate | MassTransit outbox InboxState dedups on MessageId (Plan 03-01 pattern). Verified by `ManualWalletCreditFourEyesTests` duplicate-delivery assertion. |
</threat_model>

<verification>
**Per-task verification** — each task's `<verify>` runs its own integration test via `dotnet test tests/TBE.BackofficeService.Tests/ --filter FullyQualifiedName~<TestClass>`.

**Phase-level verification** — must pass before moving to Plan 06-02:
1. `dotnet test tests/TBE.BackofficeService.Tests/ --filter "FullyQualifiedName~Phase06" --no-restore` — all 6 tests green.
2. `dotnet test tests/TBE.sln --no-restore` — entire backend suite green (no regressions in Phase 3/4/5 tests from new migrations).
3. `pnpm --filter ./src/portals/backoffice-web run build` — portal builds with no TS errors.
4. `diff -r src/portals/b2b-web/components/ui src/portals/backoffice-web/components/ui` — exit 0 (Pitfall 17 byte-identity).
5. Manual: `docker compose up keycloak` and verify `tbe-backoffice` realm imports cleanly with 4 ops-* roles (VALIDATION.md §Manual-Only Verifications).
6. Grep-assertions in PLAN (commit-time):
   - `grep -r "js.stripe.com" src/portals/backoffice-web/` → no matches (D-47 Stripe-free backoffice).
   - `grep -r "AddAuthenticationSchemes(\"Backoffice\")" src/services/BackofficeService/` → matches in every policy (Pitfall 4 pin).
   - `grep -r "DENY UPDATE" src/services/BookingService/BookingService.Infrastructure/Migrations/` → matches in `20260601100001_AddAppendOnlyRoleGrants.cs`.
</verification>

<success_criteria>
- [ ] 6 VALIDATION.md tasks (6-01-01..06) all green, `nyquist_compliant` → `true`
- [ ] BO-01, BO-03, BO-04, BO-05, BO-09, BO-10 closed per REQUIREMENTS.md acceptance criteria
- [ ] D-39 shipped: manual wallet credit with 4-eyes + self-approval guard + atomic outbox write
- [ ] BookingEvents table rejects UPDATE/DELETE at engine level (verified test, not just code inspection)
- [ ] BackofficeService has 4 role policies, MassTransit ErrorQueueConsumer bound to 10 enumerated _error queues
- [ ] backoffice-web portal boots against tbe-backoffice realm with slate-900 accent, no Stripe CSP, 4 role gates
- [ ] All cross-project interfaces (`IBookingEventsWriter`, `BackofficeEvents` contracts) live in shared projects — no circular refs
- [ ] Gateway routes `/api/backoffice/*` through `BackofficePolicy` with `Backoffice` scheme pin
- [ ] Plans 06-02/03/04 unblocked: BackofficeService bootstrap ready, BookingEvents available, DLQ pattern reusable, 4-eyes state-machine reusable for commission-payout approvals
</success_criteria>

<output>
After completion, create `.planning/phases/06-backoffice-crm/06-01-SUMMARY.md` covering:
- What shipped (6 requirements + D-39 + Keycloak realm)
- Decisions made during execution (any deviations from this plan + rationale)
- Pre-deploy gates (Keycloak realm import, SQL login provisioning for `tbe_booking_app`)
- Open acceptance criteria (if any deferred)
- Commit hashes per task
- Test counts added (target: 6 Phase06-tagged tests green)
- Affects/Provides/Patterns for history-digest consumption by Plans 06-02/03/04
</output>
