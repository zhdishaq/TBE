---
quick_id: 260424-2d9
slug: complete-opentelemetry-wiring-call-addtb
description: Add builder.Services.AddTbeOpenTelemetry(...) to the 5 Program.cs files still missing it (SearchService, HotelConnector, Crm, Backoffice, TBE.Gateway). The other 5 services are already wired.
date: 2026-04-23
mode: quick
---

# Quick Task 260424-2d9: Complete OTel wiring

## Findings

`TBE.Common.Telemetry.TelemetryServiceExtensions.AddTbeOpenTelemetry(IConfiguration, serviceName)` already exists and handles tracing + metrics with a `SensitiveAttributeProcessor` that strips PCI / PII tags before export (COMP-06 / T-03-05). 5 of 10 Program.cs files already call it; 5 do not:

| Program.cs | Status | serviceName to use |
|---|---|---|
| BookingService | ✅ wired (line 185) | `"BookingService"` |
| PaymentService | ✅ wired (line 158) | `"PaymentService"` |
| FlightConnectorService | ✅ wired (line 77) | `"FlightConnectorService"` |
| PricingService | ✅ wired (line 72) | `"PricingService"` |
| NotificationService | ✅ wired (line 70) | `"NotificationService"` |
| **SearchService** | ❌ missing | `"SearchService"` |
| **HotelConnectorService** | ❌ missing | `"HotelConnectorService"` |
| **CrmService** | ❌ missing | `"CrmService"` |
| **BackofficeService** | ❌ missing | `"BackofficeService"` |
| **TBE.Gateway** | ❌ missing | `"TBE.Gateway"` |

Every target file already has `using TBE.Common.Telemetry;` and its `.csproj` already `ProjectReference`s `TBE.Common.csproj`. No csproj edits needed.

## Locked conventions

- **serviceName** — PascalCase project short name, matching the Serilog `Enrich.WithProperty("Service", "…")` string already present in each file (e.g. SearchService → `"SearchService"`, gateway → `"TBE.Gateway"`).
- **Placement** — right after `builder.Host.UseSerilog(...)` block, before domain service registrations. Keeps tracing instrumented for subsequent registrations' dependency resolution. Matches the position used in PaymentService / BookingService / PricingService (all call it near the top of the container block).
- **One-liner only** — `builder.Services.AddTbeOpenTelemetry(builder.Configuration, "<name>");` with a short leading comment matching the project idiom.

## Must-haves

1. All 5 previously-missing Program.cs files call `AddTbeOpenTelemetry` exactly once each with the PascalCase service name.
2. No behaviour change in the 5 already-wired services.
3. `dotnet build` succeeds across the solution (or per-project if no .sln).
4. `grep -l "AddTbeOpenTelemetry" src/**/Program.cs` → 10 matches (was 5).

## Tasks

### T1 — Add call site to 5 Program.cs files

Files + placement anchor:

1. `src/services/SearchService/SearchService.API/Program.cs` — insert after the `Enrich.WithProperty("Service", "SearchService")` block, before `AddTbeMassTransitWithRabbitMq`.
2. `src/services/HotelConnectorService/HotelConnectorService.API/Program.cs` — insert after Serilog enrichment, before `AddTbeMassTransitWithRabbitMq`.
3. `src/services/CrmService/CrmService.API/Program.cs` — insert after Serilog enrichment, before `AddDbContext<CrmDbContext>`.
4. `src/services/BackofficeService/BackofficeService.API/Program.cs` — insert after Serilog enrichment, before `AddDbContext<BackofficeDbContext>`.
5. `src/gateway/TBE.Gateway/Program.cs` — insert after Serilog enrichment, before the first `AddAuthentication` call.

Each insertion is a comment line (`// Shared OTel + PII/PCI scrubbing (COMP-05 / COMP-06).`) plus the call.

### T2 — Build check

```
dotnet build
```

Target: no errors introduced. Any pre-existing warnings stay as-is.

### T3 — Verify

- `grep -rcn "AddTbeOpenTelemetry(builder.Configuration" src --include=Program.cs` — expect count of 10.
- Confirm no changes to `TelemetryServiceExtensions.cs` / `SensitiveAttributeProcessor.cs`.

## Scope boundaries

- Not in scope: changing the helper, the PII scrubber, OTLP endpoint values, or adding exporters (e.g. Prometheus / Jaeger). Adding a collector to docker-compose is a separate task.
- Not in scope: refactoring the 5 already-wired services (their serviceName strings are already inconsistent with the one in Serilog's `Enrich.WithProperty("Service", …)` for BookingService / Payment / FlightConnector — untouched).

## Done criteria

- 10 total `AddTbeOpenTelemetry` call sites in Program.cs files.
- `dotnet build` → 0 errors.
- PLAN + SUMMARY committed; STATE.md Quick Tasks Completed row added.
